packages:
  - apps/*

# Native binary used by the Next.js ESLint resolver. Declared explicitly so that
# `pnpm install` never stops for approval in CI or on Vercel (spec §13).
allowBuilds:
  unrs-resolver: true
