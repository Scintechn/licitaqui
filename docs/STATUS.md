# Status log

One line per task: date · task ID · status · PR link · follow-ups.

| Date | Task | Status | PR | Follow-ups |
|---|---|---|---|---|
| 2026-09-18 | B8 (`sync_awards` + awards backfill) | merged | https://github.com/Scintechn/licitaqui/pull/29 | **4,344 awarded items stored, not the 5,000 the card asks for** — the run stalled at 23:04 BRT when PNCP's `pncp-resultados` breaker opened (880 s) and the consulta host went down again, the same outage recorded against ADR-0001 yesterday; the backfill is committed and re-runnable, so the remaining ~650 rows are ~4 min of collection at the 165–175 rows/min it was sustaining, not a rewrite. Cost of what was collected: **2,988 result requests + 1,100 items requests + ~40 search pages ≈ 4,130 calls** at 4 req/s for 3,635 distinct awarded items (3,186 usable `OK`). **The CPF rule found a real leak and it was not the one expected:** a **MEI's razão social is the proprietor's name followed by their CPF** ("FULANO DE TAL 25510225840") on a record that is `PJ` with a valid CNPJ, so every company rule correctly said *company* and stored a raw CPF — 1 row in 4,344. `mask_embedded_cpf` now scrubs standalone 11-digit runs from every string on both branches, and `--repair-personal-data` fixed the row already written. Verified over the whole table: **zero bare CPFs in any column of any row, `raw` included**. `awarded_on` **is** exposed to the naive-Brasília trap and worse than B2's columns — a `date` loses a whole day, not three hours — handled in `award_date`; it is latent today because `dataResultado` is date-only in all 420 cached records, so it would have started misdating silently the day PNCP adds a time. Full suite **633 passed, 0 skipped** (20:25). Divergences from POC 3 in `worker/README.md`. **Latent, for whoever next touches `worker/tests/conftest.py` deliberately:** its `_delete_*` helpers connect without a `statement_timeout`, so a lock held by a concurrent lane blocks cleanup indefinitely — one full-suite run here hung 79 min on exactly that before a clean re-run; `options="-c statement_timeout=30000"` (what `licitaqui.db.connect` already uses) turns a silent hour into a fast failure. **Open for Sci:** the six "concierge segments" are a placeholder — S3 picks the 20 founders on 10-08, and `AWARDS_SEGMENTS` retargets the collection without a code change |
| 2026-09-20 | B4B (download + extract, the on-demand half of B4) | merged | https://github.com/Scintechn/licitaqui/pull/31 | **The screening path completes end to end again.** `ai_screening.load_document` gained a last source: with no pages/path/URL in the payload it calls `documents.ensure_documents(conn, tender_id)`, which downloads the active Edital and Termo de Referência, extracts them with C1's port, stores PDF and text in S3 and writes `sha256`, `s3_key`, `pages`, `text_version`, `no_text` on `tender_files` (§6.1). Wired **inside** `ai_screening` rather than as a prerequisite job or a chain, so the row the web already enqueues is the one that now works — **no change in `apps/web`**. `extract_text` (§7.1) is registered too, priority 9, for sampling; both go through `ensure_documents`. Downloads: 120 s as a **wall-clock deadline** (an httpx read timeout cannot catch the 929 s stall), 64 MB cap, 2 req/s, `pncp-download` breaker, a 4xx recorded as a breaker success so one dead annexe cannot stop every tender. **All-or-nothing**: a document that cannot be read raises instead of writing a partial analysis under a `files_hash` that claims it read everything. **`MANIFEST_VERSION` was deliberately not bumped** — folding `sha256` into B4's digest would re-analyse every tender at full price and still not detect a silent byte swap, because §3.2 caches the PDF "until the file list changes" and nothing re-fetches; closing that needs a re-validation policy, which is its own decision. **Still open for Sci:** §3.2 says the text lives "in the DB" as well as S3 and `tender_files` has no text column — a `text jsonb`/`text_key` column is a migration PR; `S3_BUCKET`, `AWS_REGION`, `AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY` need documenting in `.env.example` (a deny rule blocks agents from that file) and setting on the worker, or it runs on a `NullStore` and re-extracts every time; **Reconciled with FH (#27) on merge:** FH's `resolve_files_hash(conn, tender_id, payload)` is the one that survives — I had written a different function of the same name because the brief said it already existed at my base, and it did not. Mine is gone: its precedence was already what FH's `load_document` does with its `files_hash` parameter. `load_document` gains a **fifth** source (the tender's own documents) behind FH's four, and that branch prefers the digest `ensure_documents` computed from the snapshot it chose the documents from — a `sync_files` landing between the two reads would otherwise key an analysis under one list while having read another. FH's `EMPTY_MANIFEST_DIGEST -> None` rule is **narrowed, not dropped**: it guards the four sources where an empty `tender_files` sits beside a document the list never mentioned, while in the fifth the empty list *is* what was read, so it is the honest key and the one the cache probe must look up. the 90-day PDF sweep is designed for (`storage.describe_retention()`) but is its own card |
| 2026-09-18 | B4 (`sync_files`, list only) | merged | https://github.com/Scintechn/licitaqui/pull/23 | Amendment invalidation is by **cache key, not delete**: `files.files_hash_for()` digests the active file list and `ai_analyses` is already unique on `files_hash`, so a superseded analysis simply stops being a hit while staying on the row. **Nothing calls `files_hash_for()` yet** — whoever owns the `ai_screening` enqueue site (C1/R1) must pass it, or the invalidation is inert in production. The digest covers the *list*, not the file bytes: an agency swapping bytes at the same URL without moving `dataPublicacaoPncp` goes undetected until the download card folds `sha256` in under `MANIFEST_VERSION` 2. No migration — the 12 h TTL marker lives in `events` as `sync_files:<tender_id>` (B2's watermark precedent) because `tender_files` has no timestamp column; a `synced_at` column is the honest fix. The 25 integration tests **run for real in CI**: the `TEST_DATABASE_URL_B4` secret already existed and this PR adds the missing line to the `integration (Neon)` job from #21 — `536 passed, 20 skipped in 608s`, B4's file all dots. That job is nevertheless **red, and not for a B4 reason**: all 20 skips are `TEST_DATABASE_URL_E2 is not configured`, which #22 merged without wiring. Adding the env line alone will not fix it — **Sci needs to create E2's test database and secret**, otherwise every PR's integration check stays red |
| 2026-09-18 | C1 (`ai_screening` + POC 4 port) | merged | https://github.com/Scintechn/licitaqui/pull/15 | Extraction, page selection and prompts reproduce the POC **byte for byte** on all three answer-key PDFs, and this harness rescores the POC's own stored answers at exactly 57/58. Live: **58/58 (100%)**, 22/22 cited pages verified, R$ 0,0156 spent across two full runs. Fixed a real bug inherited from the POC's `meses()` ("12 (1 ano)" → 144 months). Model varies ±2 checks run to run (the POC's own runs did too), so the 95% gate has ~3 checks of headroom. **`OPENROUTER_API_KEY` is not a repo secret yet**: until it is, `evaluate-ai.yml` replays recorded answers instead of running live |
| 2026-09-17 | Session 1 (bootstrap) | done | — | see open items below |
| 2026-09-17 | Vercel first deploy | done | — | Fixed pnpm `allowBuilds`; deployment `dpl_F9xnFqY…` READY, build 24s |
| 2026-09-18 | B2 (`sync_open_tenders`) | merged | https://github.com/Scintechn/licitaqui/pull/13 | SP sweep 5,193 tenders in **2.09 min** (budget 30); rerun 0 duplicates; 144 tests. Sweep exercised the **search fallback** — consulta was down — so the primary path is measured only by tests. Found 4 real bugs, one of which was also in the A3 seed (now fixed) |
| 2026-09-18 | B6 (CNAE → segment map, **gap G6**) | merged | https://github.com/Scintechn/licitaqui/pull/16 | 19/20 acceptance. Sci owns the 150-code review of `db/reference/cnae_segments.csv`. Found a real item-side gap: beverages (NCM ch. 22) fall into "Outros" |
| 2026-09-18 | C1 (AI screening port) | merged | https://github.com/Scintechn/licitaqui/pull/15 | **58/58 (100%)** vs the POC's 57/58; R$ 0,0156 spent. CI gate runs in **replay** until `OPENROUTER_API_KEY` is a repo secret |
| 2026-09-18 | B5 (`company_lookup`) | merged | https://github.com/Scintechn/licitaqui/pull/12 | 49/50 resolved, 1 real failure exercised the manual path. Contradicts parts of ADR-0002 (now amended). Rate-limit *ceiling* still unmeasured — lookups stay single-threaded |
| 2026-09-18 | O1 (`/admin` + events) | merged | https://github.com/Scintechn/licitaqui/pull/11 | Needs `ADMIN_EMAILS` + `ADMIN_PASSWORD` set before `/admin` opens at all (fails closed by design). Usage card is half real: this database's size from SQL, project storage and CU-hours need `NEON_API_KEY` |
| 2026-09-17 | B1 (worker skeleton) | merged | https://github.com/Scintechn/licitaqui/pull/9 | 68 tests. **GHCR push + Easypanel deploy NOT done** — still blocked on credentials, `TODO(B1)` in ci-worker.yml stands. Neon compute *suspension* itself unproven (needs 5 idle min + the console); the no-open-session property that causes it is proven |
| 2026-09-17 | Security: migrator credential rotated | done | — | The test DSN (migrator role) was rendered into a subagent transcript by a pytest traceback. Rotated, old password verified rejected, GitHub secret updated. No file or commit was affected |
| 2026-09-17 | F1 (founders signup) | merged | https://github.com/Scintechn/licitaqui/pull/8 | Stacked on #5. Rate limiting is in-memory, not the Postgres store spec §3.3 wants — no `rate_limits` table exists yet; swap touches only `lib/rate-limit.ts`. LGPD consent version/timestamp live in `events.props` for want of a column |
| 2026-09-17 | migrate.yml + db/README | merged | https://github.com/Scintechn/licitaqui/pull/7 | Preview-branch migration job still blocked on Neon preview branching (rest of A2) |
| 2026-09-17 | B0 (spikes G7, G8) | merged | https://github.com/Scintechn/licitaqui/pull/6 | **G7 default overturned, accepted by Sci:** B2 builds on `/contratacoes/atualizacao`. G8: BrasilAPI confirmed. Open: BrasilAPI rate limit unmeasured; re-measure G7 hourly over a day before B2 commits deeply |
| 2026-09-17 | D2 (Offer page) | merged | https://github.com/Scintechn/licitaqui/pull/5 | Follow-ups: reconcile near-duplicate i18n keys with E0 before 09-24; `Field` inputs are 15px and iOS Safari zooms below 16px on a public page (D1 call); footer `[CNPJ]` omitted; privacy/terms links still anchor to `#topo` |
| 2026-09-17 | A3 (migrations + seed) | merged | https://github.com/Scintechn/licitaqui/pull/4 | Migrations applied to `neondb` (22 tables); seed is dev-only. Drizzle `schema.ts` left to R1 |
| 2026-09-17 | G3 (legal texts) | drafted | — | Privacy policy + terms copied to `docs/legal/`. **Not publishable yet**: 19 placeholders unfilled and a lawyer must review §11 liability cap, §6 price clause, §15 forum |
| 2026-09-17 | A2 (Neon roles + extensions) | partial | — | Extensions and least-privilege roles done and verified. Remaining: `dev` branch, preview-branch automation, `pg_dump` stub — all need Neon console/API access |
| 2026-09-17 | D1 (design tokens) | merged | https://github.com/Scintechn/licitaqui/pull/3 | 34 tests; Tailwind v4 `@theme` tokens, 12 components, `/dev/components` |
| 2026-09-17 | A1 (CI workflows) | merged | https://github.com/Scintechn/licitaqui/pull/1 | Make `gitleaks` a required status check on `main`; add GHCR push + Easypanel webhook with B1; Sentry DSNs still pending |
| 2026-09-17 | E0 (message texts) | merged | https://github.com/Scintechn/licitaqui/pull/2 | 10 `TODO(Sci):` open, mostly blocked on gap G3 (privacy policy + terms); price-change notice needs legal review |
| 2026-09-17 | Commit attribution fix | done | — | All 5 commits re-authored to Scintechn <development@scintechn.com> (were mis-attributed to `scintylla`). Local safety tag `backup-before-author-fix`, not pushed |
| 2026-09-18 | B3 (`sync_items` + segments) | merged | https://github.com/Scintechn/licitaqui/pull/17 | Classification matches POC 1 on 489 real cached items (labels generated by the POC, re-derived from it locally), with **one deliberate divergence**: beverages (NCM chapter 22) are rescued from "Outros" — B6's finding, 8 of 8,861 items move, all beverages. Segment values stored are POC 1's pt-BR labels, matching B6's `cnae_segments.segment` so R1 can join them (the classifier works in ASCII keys internally). Neither PNCP bug applies to items: item `orcamentoSigiloso` is a real boolean, and no `tender_items` timestamptz is fed by PNCP. Naive-time and value-zeroing details in `worker/README.md` |
| 2026-09-18 | E2 (WhatsApp founders welcome, **gap G9**) | merged | https://github.com/Scintechn/licitaqui/pull/22 | **Nothing was sent and no live Evolution call was made** — not even to Sci's number. Delivery is off behind `WHATSAPP_DELIVERY`, which only the exact word `send` turns on; a test asserts that with it off the whole job runs while `socket.connect` raises and nothing attempts a connection. The only Evolution instance on the server is `flowdeski-scn-real-estate` (another product), so **end-to-end delivery is unverified** until a LicitaQui instance exists: the `POST /message/sendText` body shape, the Chrome `User-Agent` against the live Cloudflare edge, and whether a message actually arrives. Consent (`founders_list.contact_consent`) is enforced before anything is built; `SAIR` stops further messages. `optout-confirmation` cannot be sent yet — it still carries a `TODO(Sci):` and an undecided `{{email_contato}}` (E0 §7); the opt-out is recorded regardless. Delivery log is `events` rows (`whatsapp.*`), **no migration** |
| 2026-09-18 | R1 (Radar read APIs + `readOrEnqueue`) | merged | https://github.com/Scintechn/licitaqui/pull/24 | 206 web tests. `readOrEnqueue()` is §3.1 in code: fresh serve, stale serve **and** enqueue at priority 5, absent enqueue at priority 1 → 202. Contract tests drive the handlers against the 20 real PNCP seed payloads, re-inserted under a per-**run** agency and company CNPJ. **Follow-up for D3:** `pnpm db:seed` leaves `tenders.segments` null (classification is `sync_items`'s), so the Compatível group is empty on a freshly seeded database — `db/seed.py` should call `licitaqui.items.classify_all` / `roll_up`. Also open: `/api/jobs/:id` has no owner check (no owner column; the projection carries nothing private instead), the visitor cookie is an unsigned v4 UUID verified against `visitors` until U1 brings `AUTH_SECRET`, and the worker's `POST /wake` on a priority-1 enqueue is not wired |
| 2026-09-18 | FH (wire `files_hash` into `ai_screening`) | merged | https://github.com/Scintechn/licitaqui/pull/27 | Closes the B4 → C1 gap: B4 owned the digest, C1 keyed its cache on it, **nothing passed it**, so an amended tender kept serving the analysis of the superseded edital. The hash is resolved **at the execution site** (`ai_screening.resolve_files_hash`), not at enqueue: the queue sits between the two and can outlive the file list — a payload hash would key an analysis of the *new* edital under the *old* digest (permanently, since a cached `ok` is never overwritten), `jobs_dedupe` drops the payload of the request that arrives after the amendment, and a web enqueue site would have to re-implement `MANIFEST_VERSION` in TypeScript and agree byte for byte for ever. **Widened once, deliberately**: `readScreening` in the web served the newest usable row whatever list it was keyed on, so the worker fix alone was invisible to a user — it now filters on the digest the `sync_files:` marker publishes, and falls back to today's behaviour when a tender was never synced. `565 passed, 0 skipped` (worker, every task DB resolved), 212 web tests, lint and typecheck clean. **`sync_files` still does not re-screen** and should not — B4's argument holds. **Still broken for a different reason**: `requestScreening` enqueues `{tender_id}` only and `load_document` raises without a document source, so the queued job fails four times — that is the download card's seam, and it should pick up `tender_files.url` where `resolve_files_hash` now reads the list |
| 2026-09-18 | D3 (Landing, Radar, Opportunity — canvas 01–03) | merged | https://github.com/Scintechn/licitaqui/pull/28 | 300 web tests (was 294 before this lane's own 6 suites; +2 need a database and skip without one). Ported `Main.dc.html` → `/`, `Editais.dc.html` → `/radar`, `Oportunidade.dc.html` → `/radar/edital/[...id]`; verified against the source at 390 px and 1280 px in a real Chrome, and cache headers verified on `next start` (`/` = `s-maxage=600, stale-while-revalidate`, `/radar*` = `private, no-store`). **R1's follow-up is already closed** — `pnpm db:seed` now classifies, so Compatível has content. Additions outside my own files, all append-only: `Select` and a `menu` icon in the design system (canvas 01 needs both), `TenderCard.itemCount` on the wire (the board's "7 itens" was not in the contract), a branded `app/not-found.tsx`. **Open for the next lanes:** the Opportunity CTA links to `/radar/edital/<id>/triagem` (D4) and the app bar to `/conta/criar` and `/conta/alertas` (U1, E1) — all three 404 until those land, which is why the branded 404 exists; the visitor banner is drawn here because canvas 02 draws it, though plan §5 gives it to D4 (one function, `VisitorBanner` in `app/radar/radar-view.tsx`, for D4 to lift); the "não exige atestado / capital mínimo / amostra" ticks on canvas 03 are deliberately **not** rendered — they are screening findings with page references and belong to D4, and printing them from the tender header would state a legal fact about a document nobody has read |
| 2026-09-20 | D3b (Landing: the rest of `landing_radar.html`) | PR open | https://github.com/Scintechn/licitaqui/pull/37 | **333 web tests** (306 at the branch point; +27 here). `/` was the canvas-01 hero alone — about a third of the approved page. Added, in the source's order: the founders strip, the nav and its three section anchors, the **example Radar panel**, `Como funciona`, the opportunity example with the locked price band, the Telegram alert, the three plans, the guarantees, the questions and a footer pointing at `/privacidade` and `/termos`; the `PRÉVIA` banner is deliberately **not** ported and a test pins its absence. **The example panel renders D3's own `TenderCardView`** over three real PNCP tenders from the POCs' answer keys, and is kept visibly an example: headed `EXEMPLO`, captioned with the date it is stated as of, countdowns frozen against that date, and **no card linking anywhere** — `TenderCardView` gained `href={null}` rather than a second tender card being built. **Seat count is F1's**: `lib/founders/seat-count.ts` counts `founders_list` on the server at revalidation (the same count `/fundadores` fetches); when it cannot be read the strip states 48 seats and claims nothing about how many are left — both branches tested. Verified at 390 px and 1280 px in Chrome (`scrollWidth === innerWidth` at both) and on `next start` (`/` prerendered, `s-maxage=600, stale-while-revalidate`, `x-nextjs-cache: HIT`; `/radar` still `private, no-store`). **Two class-conflict bugs the browser caught and `lib/cn.ts` predicts:** `hidden min-[900px]:inline-flex` lost to an `inline-flex` already in the shared class string and shipped the nav visible at 390 px (document 97 px wider than the screen), and a caller's `text-blue` lost to `SectionLabel`'s `text-ink` — fixed at the source (display utility per link; `SectionLabel` gained `tone="accent"`). **Open for Sci:** the desktop source's headline ("Quais licitações abertas combinam com a sua empresa?") was **not** adopted — the page keeps D3's approved canvas-01 title, because rewording a shipped `messages.radar.landing` key other lanes read is a copy call, not a port; `Entrar` points at `/conta/criar` (U1) and 404s until U1 lands; the Pro card carries `Em breve` and **no CTA**, since the source's button leads nowhere; `app/(public)/page-parts.tsx` duplicates the layout primitives `fundadores/page.tsx` declares inline — merging them would touch that file's footer, which the legal lane is editing |

## Open items from Session 1

| # | Item | Blocks | Owner |
|---|---|---|---|
| ~~1~~ | **G1 RESOLVED** — Neon created via Vercel: `neon-bistre-cloud` (id `rapid-mouse-53141905`), **AWS South America East 1 (São Paulo)**, Free plan, default branch `main`. Per spec §5.1 this pairs with Vercel functions in `gru1`, now pinned in `apps/web/vercel.json`. | — | — |
| ~~2~~ | **RESOLVED** — Vercel project linked; Root Directory verified as `apps/web` with `sourceFilesOutsideRootDirectory: true` (correct for the pnpm workspace). No change was needed. | — | — |
| ~~2b~~ | **RESOLVED — and the earlier diagnosis was wrong.** The project was never paused (`paused: false` on every read; `live: false` is normal — every project in this team reports it). The real cause was a **commit-author seat block**: `readyStateReason` = "the commit author doesn't have permission to create deployments", `seatBlock.blockCode: TEAM_ACCESS_REQUIRED` for GitHub user `scintylla`, with `alwaysRefuseToBuild: true` — which is why there were zero build logs. Fixed by re-authoring commits to `Scintechn` (the team owner). | — | — |
| ~~2c~~ | **RESOLVED** — commit author is now `Scintechn`. The Vercel deployment *creator* remains `scintechn-4604` (development@scintechn.com), the account owning the GitHub integration; that is a separate field from commit authorship. | — | — |
| ~~2d~~ | **RESOLVED** — the pending `scintylla` team access request was removed by Sci; no Pro seat consumed. | — | — |
| 2e | **Monorepo affected-projects skipping is ON** (`enableAffectedProjectsDeployments: true`). A commit touching only root-level files can be CANCELED with "the commit didn't affect this project" — this is what happened to the empty retrigger commit, and it looks like a silent failure. | all | agent |
| 3 | **GitHub secret scanning unavailable** on this private repo (API returns 422 — needs GitHub Secret Protection, or a public repo). Spec §12 assumes it is on; pick a substitute (e.g. gitleaks in CI) or accept the gap. | A1 | Sci |
| 4 | **Docker not installed locally.** Needed to build the worker image and run `docker compose up`. | B1 | Sci |
| 5 | **Python 3.14.7 locally, worker targets 3.12.** Docker image pins 3.12, so CI and production are correct; only the local venv differs. | B1 | Sci |
| 6 | `apps/web/styles/tokens.css` is a documented stub. | D1 | agent |
| 7 | `.github/workflows/` is empty — `ci-web.yml`, `ci-worker.yml`, `migrate.yml`, `evaluate-ai.yml` are task A1/A3/C1 scope (spec §13). | A1 | agent |

## Lesson: one shared test database, two agents

B1 and F1 ran concurrently against the same `licitaqui_test` database. B1's unfiltered
`claim()` picked up ~100 job rows created by F1's signup tests and flipped them to
`running`. B1 caught it (its race test claimed rows it had not created) and fixed it
properly: every database test now uses a job kind of its own (`b1t_<uuid>`) and claims
with that filter. The table was left empty.

That was an orchestration mistake, not an agent mistake — handing two parallel agents the
same database invites it. Either give each task its own database, or require a
task-scoped `kind` prefix in the brief. It also produced a genuinely useful production
API: `claim(conn, kinds=[...])` and `WORKER_JOB_KINDS`, so a container can be dedicated
to part of the queue.

## Integration checks (2026-09-17, live)

| Service | Result |
|---|---|
| OpenRouter | ✅ valid, usage 0, **spend limit 50** (gap G4 satisfied) |
| Telegram | ✅ `@LicitaQuiBot`, **no webhook set** — gap G9's worry is clear, E1 can claim it |
| Neon | ✅ both pooled and direct URLs connect |
| Evolution API | ✅ reachable, instance `flowdeski-scn-real-estate` state `open` — **but see the two warnings below** |
| Resend, Asaas, Sentry, Google, S3 | not configured yet — blocks E-mail, F2, observability, U1, B4 respectively |

**Evolution warning 1 — Cloudflare blocks default HTTP clients.** `evolutiondev.scintechn.com`
sits behind Cloudflare, which answers `403 error code: 1010` (browser-signature block) to
`Python-urllib`, *including on `GET /`*. It is not an auth failure and no API key will fix
it. **E2's client must send a browser-like `User-Agent`**, or every WhatsApp send fails 403.
Verified: identical request with a Chrome UA returns 200.

**Evolution warning 2 — the instance belongs to another product.** The only instance on
that server is `flowdeski-scn-real-estate`, i.e. FlowDeski's. Sending LicitaQui's founders
welcome through it would deliver from another product's WhatsApp number. This is the same
trap gap G9 flags for the Telegram bot. **Recommend a dedicated `licitaqui` instance before
E2 sends anything**, even to test contacts.

## Brand channels

- Instagram **@licitaqui** exists (2026-09-17). Marketing channel for the non-dev track
  (plan §5, task S2 influencer outreach). Note spec §12 forbids automated scraping of
  Instagram profiles — it is an outbound channel, never a data source.

## G7 re-measurement in progress (overnight 09-17 → 09-18)

The hourly probe ADR-0001 asked for is running. Early result, and it is not reassuring:

| Sample (UTC) | search | publicacao | atualizacao |
|---|---|---|---|
| 21:21 | 2/2 OK (~1.87 s) | **4/4 timeout** | **4/4 timeout** |
| 22:25 | 2/2 OK | **4/4 timeout** | **4/4 timeout** |
| direct one-off, 45 s timeout | inconclusive (different UA) | **2/2 timeout** | **2/2 timeout** |

The `consulta` host has been timing out for **over an hour** while the search host answers
normally. Every failure is a read timeout, the same signature B0 saw in its 13-minute
outage. ADR-0001's inversion rule is >10% failures over a day, or outages beyond ~2 h.

**RESOLVED by B2's verification — the endpoint choice stands, the cadence is what to
revisit.** B2 answered the open question: the search index's `data_atualizacao_pncp` does
**not** move when only items or files change. It is byte-identical to
`data_publicacao_pncp`, sub-second digits included. Evidence: of 95 cached tenders, **79
(83.2%) had a child change**; of those, **74 matched the header timestamp exactly and 0
matched the global one**. A search-only sweep would have missed all 74 **silently**.

That inverts the inversion rule. The outage is a *freshness* problem, which the queue and
breaker already absorb; falling back to search as the primary would be a *correctness*
problem, and a silent one. **Ignore my earlier hybrid recommendation** — it was written
before this evidence, and search-as-reconciliation cannot detect child changes either, so
it would only catch tenders missed entirely. Revisit cadence and retry policy, not the
endpoint.

**Outage record, for the ADR:** `atualizacao` timed out again
at 00:35 (40 s), while the search host answered in 0.7 s. That is **~3 h 14 min of
continuous timeouts** on the consulta host, against ADR-0001's ">2 h outage" trigger.

The maintenance-window explanation is now the weaker one: 21:21–00:35 UTC is
**18:21–21:35 in Brazil** — peak evening, not a maintenance slot. B0 also recorded the
same host failing at 20:21–20:34 UTC, so the disruption has run since roughly 20:21 UTC
with at most brief recovery.

Six sync cycles were missed. What this costs is delay, not data, because the watermark
only advances when a cycle completes every modality — a partial cycle is retried whole.

**What still needs deciding** is narrower than it looked: how long a consulta outage may
last before it is an incident rather than a delay, and whether a same-day outage should
raise anything in `/admin`. That is O2/observability work, not an architecture fork.

## Two defects found by B5, both worth fixing

1. **CI does not check Python formatting.** `ci-worker.yml` runs `ruff check .` but not
   `ruff format --check .`, so drift lands on `main` unnoticed — it already had, in B0's
   two probe scripts. B5's PR reformats them. **Add the format check to `ci-worker.yml`
   once #12 merges** (doing it before would put `main` red).
2. **B1's integration tests are not isolated between concurrent runs.** Confirmed
   independently by B5 and B2; B2 reproduced it against `main`'s own code with no B2 work
   in the session (2 failures in run 1, 0 in runs 2–3). **It will keep producing spurious
   red CI runs until someone owns it**, and a test suite that cries wolf is worse than one
   that is merely slow.

   Root cause, in `worker/tests/conftest.py`: the cleanup comment says deletes are "scoped
   by prefix", but the prefixes are module **constants** —

   ```python
   KEY_PREFIX = "b1-test-"
   KIND_PREFIX = "b1t_"
   ...
   delete from jobs where starts_with(key, %s) or starts_with(kind, %s)
   ```

   so they scope by *task*, not by *run*. Two concurrent runs of the same suite delete
   each other's fixtures mid-test. My per-agent databases (`TEST_DATABASE_URL_B2/_O1/_B5`)
   isolated the *new* tests but not these inherited ones.

   Fix — make the prefix per-run, ~3 lines:
   ```python
   RUN_ID = uuid.uuid4().hex[:8]
   KEY_PREFIX = f"b1-test-{RUN_ID}-"
   KIND_PREFIX = f"b1t_{RUN_ID}_"
   ```
   **Order matters:** both PR #12 and PR #13 already modify `conftest.py`, so do this
   *after* they merge or it conflicts with two green PRs. That is why it is written down
   here instead of already done.

   Third time a shared test database has caused a problem. Worth a standing rule: every
   test that writes to a shared database scopes its rows by a per-run id, not a per-task
   constant.

## Gap G6 closed (2026-09-18) — pending Sci's review

B6 (PR #16) builds the CNAE → segment map, so "Compatível / Verificar" is computable for
the first time. **572 mappings over 555 of the 1,332 IBGE subclasses; 777 deliberately
unmapped.** Unmapped is not `check`: it means the activity is outside the fourteen
segments, and the Radar falls back to keyword search.

It reversed its own design rule on measurement, which is the part worth trusting:

| Rule | `compatible` per company | 20-CNPJ result |
|---|---|---|
| strongest claim wins (incl. secondaries) | mean 5.75 of 14 | 16/20 |
| **main CNAE decides** (shipped) | mean 0.8 | **19/20** |

A small company's secondary CNAEs are whatever its accountant registered, so letting them
grant `compatible` made almost everything compatible. Secondaries still contribute the
segment at `check`.

The single miss was the agent's own expectation being wrong, not the map — and it said so
rather than adjusting the expectation to fit.

**Sci owns the 150-code review** (plan §5). Start with `db/reference/cnae_segments.csv`,
and focus where B6 says its judgement has no data behind it: the goods-versus-services
line (`8121400` *limpeza em prédios* and health divisions 86–87 are `check` — if concierge
tenders are mostly cleaning *services*, that should flip), retail 47 / wholesale 46 where
MEI/ME actually live, and the six catch-all codes left unmapped on purpose.

## Still to do after B3 and B6 land

- **Finish the per-run scoping.** PR #14 made `conftest.py`'s `jobs` prefixes `RUN_ID`-based
  but missed the other half: B2's sync tests scope `tenders` rows by a **constant CNPJ**
  (`99000000000102`), so two concurrent runs of `test_integration_sync_tenders.py` still
  delete each other's rows. C1 hit it live while B3 was running the same file. Same defect,
  different table — my fix was incomplete. The rule needs to cover **natural keys**, not
  just job keys.
- **Set `OPENROUTER_API_KEY` as a repo secret.** Without it `evaluate-ai.yml` runs in
  replay against committed recordings — it passes, and the report says `REPLAYED`, but the
  gate is not actually exercising the model. A live run costs about R$ 0,004.
- **Document `OPENROUTER_API_KEY` and `TEST_DATABASE_URL_C1` in `.env.example`** — a deny
  rule blocks agents from that file.

## The screening path is still broken end to end (found 2026-09-18 by FH)

`requestScreening` enqueues `{tender_id}` and nothing else. `load_document` requires one
of `pages` / `text_path` / `pdf_path` / `url` and raises `ValueError` otherwise, so the
queued job fails four times and lands `failed`. **A user clicking "analisar" today gets
nothing.**

This is not a defect in C1, B4, R1 or FH — each is correct within its card. It is the
seam none of them owned: B4's card says "list only; **download on demand**", and the
on-demand half was never carded. `tender_files.url` is populated and waiting.

What the missing card has to do: fetch the PDF for a tender's active documents, extract
the text (`pdfplumber`, already a worker dependency), store it per §3.2 — S3 for the PDF,
text in the database, delete the PDF 90 days after closing — and hand `load_document`
what it expects. The seam is `ai_screening.resolve_files_hash`, which already reads the
active list at exactly the right moment.

Two related gaps worth folding in:
- `readScreening` ignores `prompt_version`, so a prompt rollback can still serve a row the
  worker would not consider current.
- B4's digest covers the file **list**, not the bytes. An agency replacing a file at the
  same URL without moving `dataPublicacaoPncp` stays invisible until `tender_files.sha256`
  is folded in under `MANIFEST_VERSION` 2. The seam is ready.

## Open for Sci

- **Event name mismatch.** Spec §14 names the event `founder_signed_up`; F1 writes
  `founder_signup`. O1 kept F1's stored name (changing it was out of its scope) and
  catalogued both. Decide: rename the event and backfill, or amend §14 to match the code.
  Cheap now — a handful of rows — and it only gets more expensive once the gate queries
  and the digest depend on it. Not urgent: the founders gate counts `founders_list` rows
  directly, so the number is right either way.
- **Concierge-paying gate has no source.** §14 lists it among the six Phase 0 gate
  numbers, but no table marks the concierge cohort. `/admin` renders "sem fonte" rather
  than a fabricated 0/20. Needs either a column or a documented manual count before the
  11-06 gate review.

## Decisions (2026-09-17)

- **Auth:** Auth.js / NextAuth v5 stands (spec §5). The `neon_auth` schema the Vercel
  integration provisioned is unused; consider dropping it before launch.
- **Founders form:** **CNPJ is required**, because it determines which licitações a
  business can enter. "O que você vende" (`sells`) stays on the form but is **optional**.
  This makes the rendered form differ from the approved HTML, deliberately.
- **Input font size:** form inputs render at **16px**, not the board's 15px. iOS Safari
  zooms the viewport on focus for anything smaller, which is a real defect on a public
  page opened from a phone. Documented in `components/field.tsx`.
- **i18n duplicates:** where E0's catalogue and D2's page disagree, the approved page's
  rendered wording wins and E0's key naming wins. Reconciled in F1.
- **PNCP sync endpoint (G7):** `/v1/contratacoes/atualizacao`, not the POC search API. The
  search API caps `pagina × tam_pagina` at 10,000 against ~38,856 open tenders and silently
  ignores date filters, so a sweep cannot enumerate everything and misses changes silently;
  an outage on the period endpoint merely delays a cycle. **Inversion rule:** flip back if a
  full-day re-measurement shows >10% failures or outages beyond ~2 h. **Open question for
  B2's first hour:** does the search index's `data_atualizacao_pncp` move when only a
  tender's items or files change? If yes, the correctness argument weakens and search-only
  may win.
- **Branch protection:** not available on a private repo without GitHub Pro. Gap accepted
  — `gitleaks` still runs on every PR, advisory only. Revisit before anyone else commits.

## A2 — what was applied to Neon (2026-09-17)

Neon project `neon-bistre-cloud` (`rapid-mouse-53141905`), São Paulo, database `neondb`,
**PostgreSQL 18.6** (spec §5 says 16 — newer, all required extensions available).

Applied to the **main** branch, with Sci's explicit go-ahead:
- Extensions: `unaccent`, `pg_trgm`, `citext`.
- Role `migrator` — DDL: `USAGE, CREATE ON SCHEMA public`.
- Role `app` — DML only: `USAGE ON SCHEMA public`, `SELECT/INSERT/UPDATE/DELETE` on all
  tables, `USAGE, SELECT` on sequences, plus `ALTER DEFAULT PRIVILEGES FOR ROLE migrator`
  so tables created by migrations are automatically reachable.

Verified, not assumed: `migrator` created a table, `app` did DML on it, `app` was **denied**
`CREATE TABLE`, `unaccent('licitação') = 'licitacao'`, `pg_trgm` similarity and `citext`
case-insensitive compare all behave. Test table dropped afterwards.

Credentials are in `.env.neon-roles.local` (gitignored, mode 600) — never committed.

**Decision (2026-09-17):** the Vercel integration provisioned **Neon Auth** (9 tables in a
`neon_auth` schema). Sci confirmed spec §5 stands: **Auth.js / NextAuth v5**. The
`neon_auth` schema is unused and left untouched; consider dropping it before launch.

## Notes

- The repository lives at `/Users/sci/Claude/Projects/LicitaQui`, **not** inside the
  knowledge-base folder as the original `CLAUDE.md` §5 assumed. The pointer in `CLAUDE.md`
  is therefore an absolute path to `/Users/sci/Documents/POC Licitacao`.
- `pnpm-workspace.yaml` declares `allowBuilds: {unrs-resolver: true}` so that
  `pnpm install` never stops for approval in CI or on Vercel. pnpm 11 does **not** read
  `onlyBuiltDependencies`, and `pnpm rebuild` writes an unresolved placeholder into that
  file — verify a clean `rm -rf node_modules && pnpm install --frozen-lockfile` before
  pushing changes to it.
- Commit identity is pinned in `CLAUDE.md`. Getting it wrong does more than mis-attribute:
  a commit author who is not an approved team member makes Vercel **refuse to build at
  all**, with no build logs to explain it.
- **Regions.** Neon is in São Paulo (`aws-sa-east-1`, permanent). Vercel functions are
  therefore pinned to `gru1` in `apps/web/vercel.json` (spec §5.1). Note the *build*
  region is chosen by Vercel and may still show `iad1` — that is the build machine, not
  where functions execute, and it does not affect query latency.
- `vercel.json` lives in `apps/web/`, not the repo root, because the project's Root
  Directory is `apps/web`.
- **Frozen clocks in date-sensitive tests.** `test_integration_sync_tenders` fixed its
  PNCP records at 2026-09-17 while `sync_open_tenders` built its window from
  `datetime.now()`. The two drifted apart and on **2026-09-19** the search fallback's
  stop value overtook the fixtures, so the outage test wrote zero tenders and `main`
  went red — with no production change involved (`sync_tenders.py` was byte-identical
  to B2's original commit). A test whose result depends on the day it runs is a
  failure with a delayed fuse: pin the clock next to the data it has to agree with.
| 2026-09-20 | G3 (legal pages published: /termos, /privacidade) | in review | https://github.com/Scintechn/licitaqui/pull/52 | Closes the gap that mattered most before the founders week: `/fundadores` was **already live and collecting** name, e-mail, CNPJ and WhatsApp behind a consent checkbox that named two documents in **plain text with no links**, and the two pages 404'd. LGPD art. 8 / CDC art. 46 — an acceptance nobody could read is not informed consent. The four files from the knowledge base `legal/` are vendored into `docs/legal/` (v1.1, Sci's 09-20 rewrite) including the new `faq-cobranca.md`; every `TODO(Sci)` is gone because the brief **decided** rather than filled — no published address, no elected forum, `contato@`/`privacidade@` live. `[DATA]` = **20/09/2026**, the day the pages go live: the README ties it to "before the Offer form collects its first e-mail", and that form was already collecting. Pages render **from the Markdown at build time** (`lib/legal/document.ts`, `marked`), never transcribed, so the site and the repo cannot drift. **The drafting note is stripped at render** — each file opens with "Minuta … a ser revisado por um advogado antes de publicar", addressed to Sci and the lawyer; a contract whose first sentence calls itself an unreviewed draft argues against its own enforceability. Removed in the loader, not in Sci's Markdown. Footer now carries the §1 wording verbatim (Scint Tecnologia Serviços Ltda · CNPJ · contato@ · WhatsApp). Both routes prerender static. 324 web tests. **Not done:** lawyer review of §11/§6/§15 (brief §6, due before the first real charge), and `faq-cobranca.md` is vendored but has no `/ajuda` page yet |
| 2026-09-20 | CI: publish the worker image to GHCR | in review | https://github.com/Scintechn/licitaqui/pull/52 | Found while checking why production is empty: the database on `main` has **0 tenders, 0 companies, 0 analyses** and one `company_lookup` job **queued since 17:02 with attempts=0** — a real visitor searched a CNPJ on the live /radar and nothing has ever processed it. There is no worker running in production, and the image was never published: `ci-worker.yml` built it and stopped, with a TODO saying to add the push "once GHCR and Easypanel credentials exist". That premise was wrong for GHCR — `GITHUB_TOKEN` plus `packages: write` is enough, no configured secret. So the push now happens on every `main` build, tagged `:sha` and `:latest`. The Easypanel redeploy stays optional and skips with a message when `EASYPANEL_WORKER_WEBHOOK` is unset, so it can never redden `main`. **Still needs Sci:** point an Easypanel service at `ghcr.io/scintechn/licitaqui-worker:latest`, give it the worker env (DATABASE_URL, OPENROUTER_API_KEY, S3_*, AWS_*), and set the webhook secret. Until then M2's "worker in production syncing every 30 min" is unmet and /radar shows an empty database to every visitor |
| 2026-09-21 | R2 (Radar polish: wake, pagination, dead links) | in review | https://github.com/Scintechn/licitaqui/pull/52 | Three defects Sci found on the live site, one lane. **(1) `POST /wake` was never wired.** The consumer's `DEFAULT_POLL_INTERVAL_SECONDS = 120` is deliberate (Neon Free CU-hours) and stays; what was missing is the endpoint the worker already serves. `readOrEnqueue`'s `absent` branch now calls `wakeWorker()` (`apps/web/lib/jobs/wake.ts`) — **only** there, and only when `enqueueJob` actually inserted the priority-1 row (a `deduped` job is already queued or running, and `Consumer.drain()` empties the queue before it sleeps, so the insert that created it already woke the worker). The POST is handed to `after()`, so it is off the request's critical path by construction; it aborts at 1.5 s; every failure is swallowed; nothing logs the token. Measured against the dev server on the R2 database, 6 absent lookups each: `WORKER_URL` unset **median 1101 ms**, black-holed at 192.0.2.1 (connect hangs) **1165 ms**, refused at 127.0.0.1:9 **1090 ms** — no timeout enters the response. With a stub worker on :8899, exactly **3 wakes for 3 absent enqueues and 0 for 2 cache hits**, `Authorization: Bearer …` valid, empty body. **(2) "Ver mais editais" is rendered.** The copy, the keyset cursor, `nextCursor` and `?cursor=` all existed and nothing referenced the string, so `Compatíveis 79` sat above 20 rows and 59 were unreachable. The button appends (`lib/radar/pagination.ts`), hides at `nextCursor: null`, and aborts its page when the filters change under it. **The tab badge is deliberately left alone while a page loads** — it is the total, not the row count, and blanking or spinning it would claim the total is being recounted and flicker all three tabs; progress goes in a new `aria-live="polite"` line, "Mostrando 20 de 79", which keeps its old numbers until the rows are on screen. **(3) `/conta/criar` and `/conta/alertas`** (U1, E1) are linked from the live `/` and `/radar` and prefetched as 404s — eight in one page view. All five call sites now go through `ACCOUNT_HREF` / `ALERTS_HREF` in `apps/web/lib/routes.ts` to `/fundadores`; `routes.test.ts` sweeps `app/` for any hard-coded `/conta` href. **383 web tests** (351 before), lint and typecheck green. Verified in Chrome at **390 px** (single column, `scrollWidth === 390`, full-width button, 20 → 40 on press) and **1280 px** (three columns, no overflow), and the four-page walk 20/20/20/19 to "Mostrando 79 de 79" with three `?cursor=` requests and the button gone. `lib/radar/pagination.db.test.ts` runs that same walk against the database. **Assumptions (Sci was busy, not blocked):** `docs/TO_VALIDATE.md` does not exist in this repo, so item 7 was not read — say if it lives elsewhere; `WORKER_URL` / `WORKER_WAKE_TOKEN` were **not** added to `.env.example` (the file is unreadable from this worktree's permissions) — please add them before the worker container goes up, or the wake silently no-ops; the wake deliberately does not fire on a deduped priority-1 enqueue |
| 2026-09-21 | D4 (screening screen, canvas 04 + locked price block, canvas 05) | in review | https://github.com/Scintechn/licitaqui/pull/41 | Closes the gap PR #31 left: "analisar" has produced a real analysis since it merged, and there was no screen to show it. `/radar/edital/<id>/triagem` renders a real tender's triagem **with the page of every finding** (exit criterion), `/radar/edital/<id>/preco` renders canvas 05 locked. Verified live against production data on two real PNCP editais screened for this card — `00394544000185-1-002027/2026` (Obra SAA, Jacareacanga/PA, 50 pp, nota 2/10, 8 citações, 8 conferidas, capital mínimo **R$ 169.334,68** from `rules.minimum_capital_brl`, p.35/p.18/p.17/p.4) and `10882594000165-1-000542/2026` (IFSP, nota 10/10, Exclusivo ME/EPP p.12). **Next.js will not nest a segment inside a catch-all** ("Catch-all must be the last part of the URL"), so the three tender screens share `[...id]/page.tsx` and dispatch on the trailing segment (`tender-route.ts`); the board's three addresses are unchanged. Added `GET /api/tenders/:id/screening` — the poll the `POST`'s own docstring already referred to — gated on `hasSpentOn`, so it never spends, never enqueues and never hands a shared analysis to someone who did not pay for it. `VisitorBanner` is **imported** from `radar-view.tsx`, not lifted out of it, because `task/r2-radar-polish` is editing those exact lines. 419 web tests (351 + 68). **Follow-ups:** `compute_rules` writes `minimum_capital_calculation` with Python `:,.2f`, i.e. English separators ("R$ 1,693,346.78") — rendered only as a fallback here, worth fixing in the worker; `lib/founders/signup.db.test.ts` asserts on **all** of `events`, so any earlier run of `radar.db.test.ts` (which leaves `cnpj_searched` rows) makes it fail — pre-existing, clears with a `delete from events where name='cnpj_searched'`; `VisitorBanner` to be lifted into its own file once R2 lands |
| 2026-09-21 | U1 (accounts, sessions and quota enforcement) | in review | https://github.com/Scintechn/licitaqui/pull/45 | Auth.js (NextAuth v5) + Postgres, **Google now**, magic link behind `AUTH_MAGIC_LINK=1` until G2 verifies a sending domain. The adapter (`lib/auth/adapter.ts`) is **ours, not `@auth/pg-adapter`**: migration `0001` already ships `accounts`/`sessions`/`verification_token` with Auth.js's camel-case columns, but `users` is §6.2's and has no `"emailVerified"` or `image` — the stock adapter would have needed a migration, in its own PR, before U1 could ship at all. **So U1 needs no migration.** It also stores less: the Google profile photo is never written (§12 — nothing shows one). **Exit criterion demonstrated** in `lib/auth/u1.db.test.ts` through the real route handlers: device A searches a CNPJ, spends both screenings (3rd → 402), the CNPJ's window ages past 3 days, then a request with **no cookie at all** — new `visitors` row, zero `usage` rows, the literal incognito case — searches the same CNPJ and is refused **403 `visitor_expired`** before spending anything. Mutation-checked: deleting the per-CNPJ half of `windowStartedAt` fails that test. Básico's 5/month, the files unlock, session expiry and account separation are all driven through the routes too. `lib/auth/viewer.ts` is the one seam that answers "who is asking"; the visitor path is byte-identical (R1's 33 tests unchanged). `routes.ts`: `ACCOUNT_HREF` → `/conta/criar`, `ACCOUNT_PATH` added, **`ALERTS_HREF` left at `/fundadores`** (E1 owns `/conta/alertas`) and `PLAN_HREF` too (F2). **514 web tests** (451 at D4), lint, typecheck and build green. Verified in Chrome at **390 px** (buttons stack, `scrollWidth === clientWidth` on `/conta`, `/conta/criar` and `/radar`) and **1280 px**. **Google sign-in walked to Google's real consent screen** ("Choose an account … to continue to LicitaQuiApp", PKCE S256, scope `openid email profile`) and **stopped there on purpose** — completing it creates an account and grants an OAuth authorization, which is Sci's to do, not an agent's. Port 3210 was refused with `redirect_uri_mismatch` and 3000 was accepted, so **`http://localhost:3000/api/auth/callback/google` is already registered and 3210 is not**. **Needs Sci:** register `https://<production>.vercel.app/api/auth/callback/google` in the Google Cloud Console; add `AUTH_SECRET`, `AUTH_GOOGLE_ID`, `AUTH_GOOGLE_SECRET` to Vercel (and optionally `AUTH_REDIRECT_PROXY_URL`, `AUTH_MAGIC_LINK`, `AUTH_EMAIL_FROM`) plus the same names to `.env.example`, which is deny-listed here; a `TEST_DATABASE_URL_U1` would stop U1's suite sharing a database with F1's. **Assumptions (Sci busy, not blocked):** screenings stay counted **per device** — §17 decision 5 and terms line 42 scope "por aparelho e por CNPJ" to the **3 days**, and widening it to the allowance broke two of R1's merged tests, so it is written up as a commercial question, not taken; a founder's seat is copied to `users.founder_seat` on every sign-in and the **plan stays `basico`** (the seat is the right to buy Promocional at F2/M5, not the plan itself — setting `promocional` would give 48 people Essencial free for six weeks). **Fixed out of scope:** F1's `select name from events order by id` is now scoped to its own rows — the pre-existing cross-suite bug D4 flagged, which U1's new suite also trips |
| 2026-09-21 | Validation round 2 (Sci's decisions on TO_VALIDATE) | in review | https://github.com/Scintechn/licitaqui/pull/52 | Applies Sci's rulings. **Refunds reach the product for the first time**: a catalogue sweep for `devolv|reembols|garantia|arrepend|estorno` had returned **zero** strings while both promises were contractual under terms §8 and `faq-cobranca.md` said the Offer must carry them. Pasted verbatim, never paraphrased, as a section under the price, plus a one-line guarantee beside the CTA because it is a reason to subscribe. Sweep B: `radar.landing.guarantees[2]` said "renovação", the contract says "cobrança". Sweep C: `notifications.billingHelp` listed two un-disableable messages, now five. Sweep E: `founderValue._meta.verifiedOn` records when the competitor claims were last checked — comparative advertising goes stale without anyone touching the code. Legal files re-vendored: brief v1.4 (missed-reminder policy), faq v1.4 (NFS-e bracket gone at source, so /ajuda is unblocked), privacy v1.2. Items 1, 4, 5, 7, 8, 9 closed. **Open:** item 6's `refund_kind` vs the `refund_reason` already live in `0004` — rename in the billing migration, table is empty |
| 2026-09-21 | Terms v1.3 + brief v1.6 (product framing replaces the lawyer review) | in review | https://github.com/Scintechn/licitaqui/pull/52 | Sci decided there is no budget for legal review before the first charge, so the exposure is managed by how the product describes itself. Brief §2.2 adds six rules (never promise an outcome; a compatibility verdict is a reading, not a judgement; a price is an estimate with its arithmetic visible; every fact cites its page; the AI notice on every result screen; keep the evidence trail). Terms §2 rewritten to say the same in the customer's language — **does not bid, does not decide, does not recommend a price** — and `/termos` renders from this file, so merging publishes it. Version history **kept rather than reset**: the source collapsed it to one row, but the page was live on 20/09 and amended on 21/09, and §2.2 rule 6 asks for exactly that trail. Framing sweep of `pt-BR.json` recorded in TO_VALIDATE: one breach (`radar.opportunity.whyTitle` = "Por que você pode participar"), two borderline ("costuma vencer"), one prospective (no price carries "estimativa a partir dos dados do edital" — harmless only because the band is locked) |
| 2026-09-21 | E1 (Telegram linking + weekly digest) | in review | https://github.com/Scintechn/licitaqui/pull/48 | **No migration.** `telegram_links` (0001) has no column for a token's expiry, so the `/start` token carries its own: 4 bytes of expiry + 12 random, HMAC'd with a key derived from `AUTH_SECRET` under a fixed label, 42 chars — inside Telegram's 64-char `start` limit. Single use is the column (`start_token` holds `sha256(token)`, cleared in the same statement that sets `chat_id`); re-issuing supersedes. `POST /api/telegram/webhook` compares `X-Telegram-Bot-Api-Secret-Token` with `constantTimeEquals` (the same one `lib/admin/auth.ts` uses), **fails closed** on an unset or <16-char secret (503), and never calls the Bot API — it enqueues `send_telegram` keyed `reply:<update_id>` at priority 1 and calls `wakeWorker()`, so the greeting lands in ~1 s instead of up to the 2-minute idle poll. The digest is a **fan-out**: scheduler `weekly_digest` Monday 07:00 BRT → one `send_telegram` per account keyed `digest:<user_id>:<ISO week>`, so `jobs_dedupe` makes two sweeps one message and all four consumers work; tenders are chosen at send time, never in the payload. **New kill switch `TELEGRAM_DELIVERY=send`**, same word and shape as `WHATSAPP_DELIVERY` and deliberately separate — `@LicitaQuiBot` is live, so turning WhatsApp on must not turn Telegram on. §10 read from `plan_limits` (`alert` 1/week, `keywords` 1, `states` 1), week boundary in **Brasília** (a UTC one falls 21:00 Sunday in SP). §12: chat ids never logged — `telegram.chat_ref()`, the `company.cnpj_ref` shape. `/conta/alertas` built, so `ALERTS_HREF` → `ALERTS_PATH` (routes.ts) and the bell stops going to `/fundadores` (closes half of TO_VALIDATE item 7). **570 worker unit tests** (508 before), **597 web** (518 before), build green. **Post-review fix.** `TEST_DATABASE_URL_E1` was provisioned and the 17 integration tests ran for the first time; two failed, with one cause. `digests_sent_this_week` counted a dry run (deliberately, so a kill-switched machine does not loop) while `record_deliveries` fired only on a real send, so with `TELEGRAM_DELIVERY` unset — the default, and the state of production — every digest spent the week's quota and wrote nothing to `alert_deliveries`, and next Monday re-offered the same three tenders forever. `COMPLETED_EVENTS` now names the pair once and `Delivery.completed` asks the same question of an outcome; a failure still records neither. **17/17 integration, 575 unit.** **Follow-ups for Sci:** (1) the webhook is still unregistered — `setWebhook` is a production change, and a Vercel preview URL rotates so it can only point at production; (2) `plan_limits` has an `alert` row for `basico` only, so the code reads a missing cap as *uncapped* rather than zero (else founders on `promocional` get nothing on opening week) — wants rows in a migration; (3) `TELEGRAM_CHAT_ID` in `.env.local` is **the bot's own id** (8619343039), not a chat — nothing reads it, and Sci is replacing it; the 403 it produces is now a named reason code. Telegram delivery itself **is** proven (Sci pressed Start; a real message reached chat 958776226, `message_id=6`); (4) `u1.db.test.ts` and `signup.db.test.ts` race on the global 48 `founders_list.seat`s when run in parallel (seen once: `Key (seat)=(22) already exists`, green on re-run) |
| 2026-09-21 | Framing rulings applied (brief §2.2 v1.7) | in review | https://github.com/Scintechn/licitaqui/pull/52 | Sci's five rulings on the framing sweep. `whyTitle` → "Por que este edital apareceu para você" ("pode participar" is a habilitação judgement we cannot make). "costuma vencer" → "a faixa em que os vencedores fecharam" — **3 strings, not the 2 the ruling named**: my sweep listed `radar.landing.opportunity.body` under rule 1 and failed to flag it, so the ruling was made on an incomplete list. "preço-alvo" kept but never alone, always paired with "o máximo a pagar ao fornecedor para manter a sua margem" (4 strings). `radar.price.ceilingLabel`/`inputsLabel` added for ruling 4's result-screen constraint. Rule 3's full price label added to **B8's acceptance criteria**, since locked bars are the only reason it is not a defect today. Brief v1.7 vendored. TO_VALIDATE corrected: the lawyer review is **deferred, not cancelled** — no budget before the first charge, revisit after Gate 0 on 06/11; items 5 and 7 closed. Four regression tests pin rules 1-3 |
| 2026-09-22 | E3 (Telegram linking journey) carded as a launch blocker | planned | — | Found by Sci testing the real flow on production. The token was minted and the browser handed off to the bot, but `telegram_links.chat_id` stayed **null**, no `telegram_linked` event was written and no reply job was queued — the `/start` never reached the webhook, so there was no message to miss. **Suspected cause, unconfirmed:** `t.me/<bot>?start=<token>` auto-sends the payload only for a first-ever conversation; a chat with history just opens. Sci's Telegram already had a chat with @LicitaQuiBot from yesterday's delivery test, which is exactly the case that fails — and during founders week anyone who ever opened the bot hits it. Two separate problems: a functional one (the flow assumes the tap always sends `/start`) and a UX one (bounced to another app, no waiting state, no failure path, and the only confirmation is *in* Telegram, which is the channel that failed). Confirm the cause by sending `/start <token>` by hand before building |
| 2026-09-22 | U2 (magic-link sign-in) carded as a launch blocker | planned | — | Surfaced while designing a journey-test persona: "someone who will not use a Google account". Today that person **cannot create an account at all** — `/conta/criar` offers Google or nothing. Magic link is **already built** (`lib/auth/config.ts`, `app/conta/actions.ts`, `sign-in-view.tsx`) and gated behind `AUTH_MAGIC_LINK` because G2 had not verified a Resend sending domain. Legal brief §1 records the domain as **verified on 21/09**, so the gate outlived its reason and nobody flipped it. Needs: confirm verification in the Resend dashboard (the repo key is send-only and cannot list domains), then `AUTH_MAGIC_LINK=1` + `RESEND_API_KEY` + `AUTH_EMAIL_FROM=noreply@licitaquiapp.com.br` in Vercel Production, redeploy, and a real sign-in from an address that is **not** the Resend account owner — the test sender silently drops everyone else, which is exactly the failure to rule out. G2's gap row and M3 updated |
| 2026-09-22 | E3 (account → Telegram linking journey) | in review | https://github.com/Scintechn/licitaqui/pull/52 | **No migration.** **The card's hypothesis does not hold, and the launch blocker is a production config mismatch only Sci can fix.** Production runtime logs show the `/start` *did* reach `POST /api/telegram/webhook` and was rejected: an unbroken stream of ~72 POSTs from **07:42 UTC onward**, spaced 60–110 s and lengthening — Telegram redelivering one un-acknowledged update, exactly as `route.ts` documents — every one answered **401 `bad_secret`**, plus **11 × 503 `not_configured`** earlier from a window with no `TELEGRAM_WEBHOOK_SECRET` set. So the `secret_token` given to `setWebhook` ≠ production's `TELEGRAM_WEBHOOK_SECRET`. **Sci: run `getWebhookInfo` (expect `last_error_message: "Wrong response from the webhook: 401 Unauthorized"`), then re-run `setWebhook` with the exact production value.** Until that is done no amount of UI fixes the link. The deep-link "returning chat swallows the payload" theory is **unproven** and nothing here special-cases it; the manual fallback covers that case and every other, without asserting a cause. E1's server path is sound and its 17 integration tests already prove it. **What shipped:** (1) `lib/telegram/handoff.ts` — a four-state hand-off (`idle`/`waiting`/`failed`/`linked`) from two facts that already exist: `telegram_links.start_token is not null and chat_id is null` (now `readLinkStatus().pending`) plus an httpOnly 15-min cookie holding the plaintext token, since the column keeps only `sha256(token)` and the manual fallback must print the real thing. The token's own signed expiry is the only clock, so no `issued_at` column. `connectTelegram` now redirects to `/conta/alertas` instead of `t.me`, which renders the deep link, the `/start <token>` to paste, a re-check and the validity window — one extra tap, bought with a hand-off that can be seen and recovered. (2) **CNPJ detour closed:** `/conta/alertas` asks for the CNPJ inline instead of sending people to the Radar. (3) **CNPJ no longer permanent:** `setUserCnpj` + `saveCompany` are the account setting `rememberUserCnpj`'s comment had been deferring to since U1; `rememberUserCnpj` is unchanged and still only fills a `null`. **Gotcha worth knowing:** `users.cnpj` is `references companies(cnpj)`, which is the real reason `rememberUserCnpj` carries its `exists` guard — so `ensureCompanyRow` inserts a key-only placeholder stamped `'epoch'` (dated *now* it would read as **fresh** and never be refilled) and `companyOrLookup` runs **first**, where an absent company enqueues at priority 1 and wakes the worker. **634 web tests** (601 on main), lint/typecheck/build green; 390 px and 1280 px checked in Chrome, no horizontal overflow. **Follow-ups for Sci:** (1) the secret mismatch above — the actual blocker; (2) a denied webhook is invisible to operators (a `console.warn` nobody watches) — worth an `/admin` health row or an alarm, O2's territory; (3) `saveCompany` writes **no** event, so a CNPJ attached from `/conta` does not count toward §14's "≥ 300 CNPJs" gate — `cnpj_searched` would be the wrong name for it, so the gate's definition is Sci's call; (4) `u1.db.test.ts`/`signup.db.test.ts` still race on the 48 `founders_list` seats (known, not mine) |
| 2026-09-22 | Legal pages: two tokens that never existed, and a guard | in review | https://github.com/Scintechn/licitaqui/pull/53 | Found by the front-end architecture review. `legal-page.tsx` shipped `bg-canvas` and `text-title`; **neither is in `tokens.css`**. Tailwind v4 drops an unknown utility silently, so nothing failed: the background looked right only by inheriting the body's ivory, and the `h1` on `/termos` and `/privacidade` rendered at the browser default on the two pages the law requires us to publish. My bug, from PR #36. Now `bg-ivory` and `text-section`. `styles/tokens.test.ts` sweeps every `text-`/`bg-`/`border-` utility under `app/` and `components/` against the `@theme` names — reintroducing `bg-canvas` fails it immediately, which is how I verified it. Tailwind's own side and width utilities (`border-t`, `border-b-2`) are excluded explicitly |
| 2026-09-22 | Pre-launch fixes from two production reviews (items 1–10) | in review | https://github.com/Scintechn/licitaqui/pull/55 | **No migration, display layer only.** Ten findings observed on the live site, verified against CNPJ `36955612000185` on production data. **(1) The headline slot.** On the first page of `compatible`, **20 of 20** cards carry no `estimated_value` — the 22px Archivo anchor read "Valor não informado" twenty times. `lib/radar/headline.ts` elects the anchor: value when there is one, else **the deadline** (the only fact that decides whether to act today, the one PNCP never withholds, and the product's own promise is "antes do prazo"), else item count, else ME/EPP regime, else nothing — the absence drops to a quiet `text-meta text-muted` line and is never the anchor. The top-right countdown steps aside when the deadline is promoted, so no card says "13 dias" twice. Same rule on `opportunity-view`, where the 28px slot collapses and the deadline block above it is already the biggest thing. **(2) Titles.** `cleanTitle()` strips the `[portal]` prefix, the trailing full stop and the shouting. Judged **per word, not per string**: agencies shout the object then paste the legal basis in prose, so one lowercase letter 300 chars in used to leave the visible 120 untouched — caught in the browser, not by a test. Acronyms and codes survive; a 4-letter acronym outside the allow-list does not, which is the accepted cost. **(3) `support.email` was `TODO(Sci)` and shipped** into `legal.dataRequest`, the LGPD channel brief §1 says must never bounce — the line was suppressed rather than wrong, which is worse. Now `contato@` for support, **`privacidade@` for data-subject requests**, and `/conta` renders the sentence again. **(4) AI notice** (§2.2 rule 5) added to `opportunity-view` (a compatibility reading) and `price-view` (money) — it was in marketing and the screening only. **(5) `--color-attention` measured 4.419:1** on its own soft fill, not the 4.9 the comment claimed against ivory: the badge is never on ivory. `#8f5200` → **5.30:1** measured, **5.299:1 confirmed in Chrome** on the 11px `VERIFICAR` badge at 320/390/1280. `success` and `muted` re-measured at 4.64:1 and left alone. New `styles/contrast.test.ts` pins all 15 ink/fill pairs the product paints text in. **(6) Seat meter:** 48 empty boxes under "Restam 48 vagas" is a picture of an empty room, and because the page is static it was **every** first paint regardless of sales. Grid now renders only above zero; verified live at 0 (no grid) and at 7 (grid returns, "Restam 41 vagas"). **(7) Bucket hints** moved into `/radar` under the tabs — they only existed on the marketing landing, which an e-mail link skips. **(8) Wordmark outlined as SVG** (2,437 B, HarfBuzz-shaped at wght 800/wdth 85, viewBox reproducing the old CSS line box exactly), so `fonts.ts` drops `axes: ['wdth']` and pins 600/700/800: **Archivo Latin 90,096 → 34,940 B (−61.2%)**, preload per route **150,448 → 95,292 B (−55,156 B, −36.7%)**, all font files −31.4%. Spec §11 updated — it required the axis. **(9) `::selection`** was the browser default lavender; now brand Blue. Graphite on that blue is **2.91:1 (fails)**, so the text is Ivory at **5.78:1**; the graphite panel gets `--color-blue-light` (text 5.19:1, fill-vs-panel 5.19:1) — both confirmed in Chrome. **(10) Density:** public section rhythm 48/64 → 40/48, **5909 → 5621 px (−288 px, −4.9%)** at 1280. **The premise did not survive measurement and the rest is Sci's call:** the signup card (1060 px) sets the hero row height, not the left column (469 px), so tightening the left column reclaims nothing above the fold and *widens* the 591 px void beside the form — capping `--text-hero` to 52px would buy a 4-line headline and make that imbalance worse. Above-the-fold element count is unchanged at 16; the hero's balance is a layout/content decision, not a spacing one. **53 test files / 705 tests** (50/638 on main), lint, typecheck and build green; 390/1280 checked and 320 for the badges, no horizontal overflow at any width, touch targets, body line-height and the 20px gutter untouched. **Follow-ups for Sci:** (a) spec §11 now says the wordmark ships as outlines — one line to revert if that is not wanted; (b) the hero balance above; (c) `u1.db.test.ts`/`signup.db.test.ts` still race on the 48 seats (known, not mine) |
| 2026-09-22 | PNCP 404 on `/itens` and `/arquivos`: stuck collector jobs | in review | https://github.com/Scintechn/licitaqui/pull/54 | Reported as "two jobs retrying forever" on `03788239000166-1-000229/2026`; **the evidence says something else and something worse.** The jobs (2286, 2287) were not looping — they were `failed` after 4 attempts, which is terminal and silent, and their last error was `CircuitOpen`, not the 404. The 404 itself is real and stable, and PNCP's Consulta detail endpoint says what it means: **`410 GONE — "A contratação informada foi excluída e não pode ser consultada."`** for all three affected tenders (also `05733936000145-1-000005/2026`, `46634044000174-1-000220/2026`). So the 404 was neither "no items" nor a wrong path: the agency **withdrew** the contratação after we ingested it. **The 404 did count toward the breaker**, but not the way it looks: `_get` called `record_success()` and then raised *inside* `breaker.guard()`, whose `except` recorded a failure anyway — invisible among 4xx alone (the success reset the counter first, so it never reached two, and the existing test passed throughout), fatal when mixed: one 404 left `pncp-itens` one failure short of open, so the next genuine timeout opened it and stopped **every** tender's items for 15 minutes. Now only a transport failure or a 5xx reaches the breaker. Second half of the bug: `sync_items` had **no "synced, empty" state** — `tender_items` has a row-level `updated_at` and zero rows to carry it, so a tender with no items read as stale on every sweep forever; it now writes the same `events` marker B4 writes for the file list. Verified in production: both jobs re-run **`done`**, markers `absent: withdrawn`. **Two follow-ups for Sci.** (1) **A withdrawn tender is still on the Radar, and completing its job made it *more* visible** — `roll_up` now runs and classified `03788239000166…` as `Alimentos` from its object text. Whether a withdrawn tender is hidden, greyed out or left alone is a product decision the spec does not cover (§3.2 has no row for it); nothing here changes what users see beyond that classification. (2) **Proposed schema PR, not shipped here** (CLAUDE.md: migrations are their own PR): `tenders.items_synced_at`/`files_synced_at` + `withdrawn_at` columns to replace the two `events` markers, and a `parked` value on `jobs_status_check` so a regression can be parked for review instead of ending as an ordinary `failed`. Three older jobs (4108, 7395, 7396) are stuck the same way and will never self-heal, since their tenders are excluded and can no longer change — left alone as out of this card's scope; re-runnable once this is deployed |
| 2026-09-22 | PNCP traceability (Id contratação PNCP, link to the official record, whole Objeto) | in review | https://github.com/Scintechn/licitaqui/pull/56 | **The detail screen can now be checked against PNCP in one click.** The Id contratação PNCP sits in the Operação block beside Modalidade and Situação — verbatim, `user-select: all`, with a labelled "Copiar" next to it rather than around it (a click inside a `<button>` does not select its text, and pasting the id into PNCP search is the use). `lib/radar/pncp.ts` is the web app's one parser for `numeroControlePNCP`; it answers `null` rather than guessing, so an unreadable id renders no link instead of a 404 with our name on it. The zero-stripping (`000958` → `958`) is not inferred: PNCP writes the same triple into the file URLs we store verbatim in `tender_files.url`. Verified live in Chrome on three production tenders, one per modality — Pregão - Eletrônico `87612826000190-1-000958/2026`, Dispensa `01298975000100-1-000157/2026`, Concorrência - Eletrônica `18663401000197-1-000233/2026` — each portal page printing our id back character for character. The **whole Objeto** now renders verbatim above the screening CTA, no toggle (§2.2 rule 4: the AI is a reading *of this text*, so the reader gets the source first), `whitespace-pre-line` to keep the órgãos' own lotes list. 54 files / 745 tests, +1 file and +40 tests over `main` at #55. **Duplicate editais: reported, not fixed** — 92 near-duplicate groups, 94 redundant listings, 2.0% of the Radar, across 77 agencies, by the rule *same CNPJ + year + item count + item sum + normalised-object similarity ≥ 0.90*; `…-000958/2026` and `…-000963/2026` are both on PNCP as "Edital nº 67/2026" and "Edital nº 0067/2026". Proposal in the PR is to **group, not delete**. Follow-ups: the list-card lane owns the same text on `tender-card.tsx` and is deliberately not sharing a component yet; `estimated_value` is missing on this tender and belongs to the next worker lane. |
| 2026-09-22 | Tender status and watching: vendor B9/B10, §2.2 rule 6, spec §10 | in review | https://github.com/Scintechn/licitaqui/pull/PENDING | Sci authored the amendments in the knowledge base; these are **cherry-picked, not re-synced**. The KB copies are an older base with today's additions layered on, so a wholesale copy would have silently reverted: cards **U2** and **E3** (both deleted there), `ends_on`/`refunded_at`/`refund_reason` from the `subscriptions` schema (migration 0004, applied in production), and the Asaas `notificationDisabled: true` requirement in two places — the rule that keeps their per-notification billing off our invoice. `CLAUDE.md` says the repository copy is the one that ships and warns against re-syncing from the knowledge base; this is why. Copied verbatim, no wording edited (brief §5): §2.2 **rule 6** (no urgency copy when `situacaoCompraId != 1`, old rule 6 renumbered to 7, brief to v1.8), plan §5 cards **B9** and **B10**, the spec's status-and-watching paragraph, and `docs/TENDER_STATUS_AND_WATCH.md` |
| 2026-09-22 | Radar **list** UX: the three failures Sci hit in one sitting | in review | https://github.com/Scintechn/licitaqui/pull/57 (merged) + https://github.com/Scintechn/licitaqui/pull/62 | All three reproduced on production with CNPJ `36955612000185` before a line changed, and measured again after. **(1) Going back threw the search away.** The query was always in the URL; the *screen* was not: `RadarScreen` re-ran its whole pipeline on every mount, so Back re-analysed the CNPJ and dropped every "Ver mais" page — 60 cards and 3000 px, back to a spinner. `lib/radar/list-cache.ts` restores the list before the first paint: every loaded page, the cursor, the tab and the scroll. **Staleness rule, from the spec and not invented:** under 60 s restored with **no request at all** (the sweep owns this data and runs every 30 min); 60 s–30 min restored and refreshed behind it (§3.1); past 30 min discarded, because §3.2 is the TTL of an open tender and `proposals_close_at > now()` is evaluated in Postgres, not in the tab. A restored snapshot keeps its **original** timestamp, so being shown again does not renew its lease. The in-app way back was separately broken: `opportunity-screen.tsx` builds "Voltar" from `cnpj`/`uf`/`q`/`group` and the cards linked to a bare `/radar/edital/…`, so it went to a bare `/radar` — `tenderHrefFrom()` carries the search. **(2) Reading the Objeto cost clicks for text we already hold:** 16 of the 20 cards on page 1 truncate at 120 chars (median 205, longest 512) and the full string is in the list payload, so "Objeto completo" is a `<details>` on the card — no request, no screening, collapsed by default, and **no control at all** on the 4 cards whose object already fits. `<details>` cannot live inside an `<a>`, so the card is now a surface holding a link and a disclosure (two keyboard stops on cards that have more to show). **(3) An empty tab:** `readGroup()` answered `'compatible'` for an absent group unconditionally, so the real query `q=pavimentação asfáltica` (`{0, 0, 39}`) opened on an empty Compatíveis. Absent now means *unchosen* and `bestGroup()` elects a populated tab from the counts the route already returns; a tab the user pressed is never second-guessed (`radarHref()` writes `group=compatible` out so the two URLs differ); the chip counts are announced with their noun instead of as a bare digit; an empty tab says "Ver 39 editais em Palavras" and links there; "every tab is empty" is its own sentence (`radar.list.allEmpty.*`). **Verified in Chrome at 390 px and 1280 px against production data** — headless over CDP for the scripted runs, a visible window for the screenshots: both ways back (browser Back, and "Voltar" as a fresh document) restore 60 cards, "Mostrando 60 de 150", scroll 3000, no analyzing card, **zero requests**; the unchosen search lands on Palavras in 3 requests and on 0 when restored; no horizontal scroll at 390. **57 test files / 802 tests**, lint, typecheck and build green (main alone: 57/792). **Follow-ups for Sci:** `radar.states.emptyBody` still says "Amplie as UFs ou adicione uma palavra-chave" on a search that already has a keyword — the action line under it now carries the useful instruction, but the sentence could be better; and the background refresh re-reads only page 1, so rows further down keep the values they were loaded with (the "atualizado há X" line above states their age). **Assumption recorded, not taken as decided:** the two thresholds (60 s, 30 min) are named constants taken from §3.1/§3.2 rather than a product decision |
| 2026-09-22 | B9 (tender status as a first-class state) | in review | https://github.com/Scintechn/licitaqui/pull/61 | **No migration, display layer only — and none was needed.** `tenders.status` already stores `situacaoCompraNome`, and **`tenders.pncp_updated_at` already stores `dataAtualizacaoGlobal`**, which is the date the banner had to cite (our `updated_at` is when *our* sweep ran and would have dated a suspension to our own cron). The numeric `situacao_id` the card names is already in **`tenders.raw->>'situacao_id'`** — confirmed `4` on the HUPE-RJ row — so B10 needs no migration for it either; B9 gates on the name. **The gate:** `lib/radar/tender-status.ts` — `mayShowUrgency(tender)` is the single predicate every urgency element asks, plus `statusNotice()` and `statusChipLabel()`. The tenth row of the Operação block *is* the scattered-conditional failure mode: status was on the screen and each element independently failed to consult it, so this is deliberately one function with 20 tests rather than an `if` per screen. **Unknown/null statuses forbid urgency** (rule 6 is an allow-list; 0 nulls and 0 unknowns across 4 919 production rows, so the cost is hypothetical and the alternative is the defect). **Every urgency element found**, including three nobody listed: the why-list's "Ainda dá tempo"; the "último dia"/"restantes" pair; the card's top-right countdown; **the deadline promoted into the card's 22px anchor slot** by `headline.ts` (19 of 20 cards carry no value, so that was the loudest place it lived); the card's "Proposta até" line, now "Data anterior"; and **`closedNotice`** — "as propostas já encerraram" is the same false temporal claim in the other direction on a tender that may resume, so it passes the same gate. Swept and cleared: no e-mail/Telegram/digest body exists under `apps/web` (only linking/quota plumbing); the landing's alert mock and `landing-example.ts` are frozen marketing examples, pinned `Divulgada no PNCP` explicitly so the gate leaves them alone; `VisitorBanner`'s "último dia" is the 3-day trial, not a tender, and is untouched. **Sorting:** non-Divulgada falls below Divulgada and is **never hidden**; `halted` had to join the keyset cursor or rows vanish at every page boundary — verified live by walking all 70 keyword matches (70 unique, the 3 stopped ones partitioned last despite *earlier* deadlines than the Divulgada tail). Legacy 2-field cursors still decode. **`status` moved onto `TenderCard`**, so the type system — not memory — makes every construction site decide; that is what caught `opportunity-view.test.tsx` carrying `'Recebendo propostas'`, a value the domain table does not have. **Contrast:** banner and chip reuse `--color-attention` on `--color-attention-soft` at **5.30:1** (already pinned); the first draft quieted the source line with `opacity-90`, which blends it to **4.37:1** and fails AA — removed, and the pairing added to `contrast.test.ts` with that note. **Fixture:** the HUPE-RJ tender itself captured from production into `lib/radar/hupe.fixture.ts` with `situacao_id` 4, serving B9's rendering now and B10's diff later. **59 files / 829 tests** (57/806 at `696d3d0`), lint/typecheck/build green. Verified in Chrome at 390 px and 1280 px on production data — suspended `46854998000192-1-000556/2026` (PMSP, closes 25/09, still open) shows the banner, the SUSPENSA chip, "Prazo suspenso"/"data anterior" and zero urgency; Divulgada control `03061303000102-1-000124/2026` still reads "2 dias / restantes" and "Ainda dá tempo" and is pixel-unchanged; no horizontal overflow at either width. **Follow-ups for Sci:** (1) **the Portuguese copy needs your ruling (brief §5)** — 15 new strings under `radar.status` plus `radar.card.previousDeadline`, listed in the PR; the load-bearing one is "Suspensão não é cancelamento: o órgão pode retomar este edital com novas datas", which exists because §3.1 forbids wording a suspension as final; (2) the Radar **list** was verified through the API, not the rendered page — React does not hydrate in this Chrome profile (an extension injects `bis_skin_checked`, visible as a hydration mismatch in the dev log), so the card's rendering is covered by tests on the real payload instead; (3) `u1.db.test.ts`/`signup.db.test.ts` still race on the 48 `founders_list` seats (known, not mine) |
| 2026-09-22 | Neon out of disk: writes failing in production | done | https://github.com/Scintechn/licitaqui/pull/PENDING | **Outage, not a warning.** `psycopg.errors.DiskFull: project size limit (512 MB) has been exceeded` while backfilling `short_title`; every insert, update and delete was failing. Project was at **496.7 MB**: `neondb` 293 MB (of which `tender_items` **231 MB** — 127,980 rows at ~1.6 KB of heap each against a **175-character** average description, i.e. upsert bloat, not text) and 16 `licitaqui_test_*` databases at 195.9 MB. Truncated every data table in all 16 test databases, keeping `schema_migrations`, `plan_limits` and `cnae_segments` — **freed 53.5 MB**, project now 445.5 MB. The number that matters is what it did **not** free: an **empty** test database still costs **8.8 MB**, so 16 of them are **141 MB of irreducible overhead — 27% of the free tier** whether a test runs or not; only ~55 MB was real data. Growth is ~35 MB/day (≈1,000 tenders/day x 22 items), so the headroom is under two days. `VACUUM FULL` on a 207 MB heap needs roughly double that free and may reclaim nothing on copy-on-write storage, so production was left untouched. Spec **§14.1** (vendored here) records the rules: CI and production never share a storage budget; check the budget before creating a database or running a backfill; `ON CONFLICT DO UPDATE ... WHERE t IS DISTINCT FROM excluded`; `tender_items` gets a retention policy; prefer lazy item fetching; a backfill must be resumable, must never swallow a `DiskFull` as success, and must measure its own storage delta. Follow-ups carded by Sci: no-op upserts, retention, lazy fetching, **CI in its own Neon project**, and an `/admin` storage alarm at 80% with a Telegram ping |
| 2026-09-22 | Tender detail: one tab strip, and the items on it | in review | https://github.com/Scintechn/licitaqui/pull/64 | Two screens of the same tender had two design systems. **After** the triagem it has tabs (`Resumo · Documentos · Exigências`); **before** it, the detail screen ended in a loose `EDITAL E ANEXOS · CRIAR CONTA` button with a bare `.zip` link hanging under the CTA — Sci: *"I believe we need to keep the same design system and have the file in the same tabs, not placed surfing anywhere."* And the thing he opens PNCP for was missing: *"we have it in the TAB ITENS… without it, I have to open two applications."* **Reused, not rewritten:** canvas 04's strip moved out of `screening-view.tsx` into `components/tabs.tsx`; both screens import it, and it grew `aria-controls`/`aria-labelledby` on the way. **Tab set: `Itens · Documentos`, Itens first** — two and not PNCP's five because we hold two tables; Itens first because it is PNCP's own first tab and the one that is never locked. Header, deadline/value card, **Objeto**, why-list and Operação all stay **outside** the tabs. **Items were already on the wire** — `GET /api/tenders/:id` has always returned `items` from our own `tender_items`; nothing new is fetched and PNCP is never called in a request (§3). **Columns** in PNCP's vocabulary: Número · Descrição · Quantidade · Valor unitário estimado · Valor total estimado, with `moneyExact()` (two decimals, never scaled — `money()` would print `R$ 2,99 mi`, which cannot be checked against anything). Verified live on `12342663000173-1-000017/2026`: 15 rows, item 1 `400 Hora × R$ 816,67 = R$ 326.668,00`, sum **R$ 2.988.571,02** — PNCP's own header figure. **Paging, measured:** production runs far past the card's "55+" — the largest tender holds **1 124 items**, 122 tenders exceed 200. 20 rows a page (`Ver mais itens` / `Mostrando 20 de 1124 itens`); in Chrome the 1 124-item tender renders **657 DOM nodes** instead of ~29 000, the whole list being >20× the markup. Virtualisation rejected on this data: row heights run from one line to an expanded 2 048-character spec, it would need browser measurement (so no SSR and no `renderToStaticMarkup` tests) and it breaks find-in-page. **390 px:** cards below `md`, a real `<table>` with `<th scope="col">` from `md` up — one of the two is always `display:none`, so exactly one is in the a11y tree and neither fakes table semantics with ARIA. Five columns cannot be read in a 390 px frame; an `overflow-x` table would make the reader scroll away from the description to reach the price. **Descriptions** over 160 chars collapse behind the same `<details>` the Radar card used, **verbatim — not de-shouted**: the column exists to be checked against PNCP's cell, and case carries meaning in a specification. **The total is never a substitute for the tender's value** — `estimated_value` is null on **5 036 of 5 921** tenders with items today and on **42** it already differs from the sum of the agency's own rows, so the figure is labelled `Soma dos N itens deste edital` with its arithmetic named (§2.2 rule 3) and omitted entirely if any row lacks a total. **Documentos:** the §8 account gate is unchanged, but the lock is now a state of the tab (a `LockedBlock` a visitor can open and read) instead of an absence outside it; `files: []` gets its own sentence. **Radar card:** `Objeto completo` removed (Sci: *"we will have the whole description inside the item"*), so the card is a single `CardLink` again — one keyboard stop per tender, the regression the `<details>` had forced. `lib/radar/object.ts` existed only for that disclosure and went with it. **B9 survives:** suspended `10735145000194-1-000138/2026` (827 items) still banners *"Edital SUSPENSO pelo órgão em 21/09/2026"* above everything and shows **zero** urgency strings, tabs and all. **Contrast, measured in Chrome on the rendered pairs:** sum label `#6b6b67` on `#f3eee8` **4.64:1**, sum figure `#171717` on `#f3eee8` **15.54:1** (pair added to `contrast.test.ts`), card figure label on surface **5.35:1**, `Ver descrição completa` `#2457d6` on `#ffffff` **6.16:1**, active tab on ivory **5.78:1**, inactive tab / column headers / unit / row number **5.02:1**. No `opacity` anywhere. **59 files / 885 tests** (59/830 at `efeaf77`), lint · typecheck · build green. Verified in real Chrome at **390 px and 1280 px** on production data: no horizontal overflow at either width on the 15-, 827- and 1 124-item tenders. **Also fixes a silent regression this PR surfaced:** PR #56 moved the **Objeto** to directly under the deadline/value box, spanning the content column at `max-w-[68ch]`, because Sci asked for it by name — *"The Objeto should be right after the box with the price… The Objeto will place the entire weight below to the box."* `task/b9-tender-status` branched **before** #56 landed and its merge `cc4b766` resolved the conflict in its own favour, putting the block back under the two-column grid, restoring `max-w-[62ch]` **and deleting the three tests that guarded the order** (ten `it()` blocks under `OpportunityView · the whole Objeto` on #56, seven on `main`). B9's own diff touches `FullObject` **zero times**, so none of it was decided — and with the guards gone CI stayed green while undoing a change Sci had asked for. Restored here: the placement, the 68 ch measure, #56's reasoning comment and all three tests, **plus a fourth** pinning the Objeto above `role="tablist"`. Verified the way #56's lane did — re-applying B9's revert fails exactly those three, and pushing the Objeto below the tabs fails the fourth. Confirmed in Chrome at both widths: the chain reads box → Objeto → Por que → Operação → tabs, and at 1280 px the Objeto is a **sibling** of the grid spanning all 920 px with the measure capped at 571 px. **Follow-ups for Sci:** (1) **20 new Portuguese strings need your ruling (brief §5)** — `radar.opportunity.tabs.*`, `radar.opportunity.items.*`, `radar.opportunity.files.*`, listed in the PR; the load-bearing one is `items.sumNote`, which is what stops the sum reading as the agency's declared total; (2) **`tenders.short_title`**: once the AI title lands the card headline is a paraphrase and the list stops carrying the real subject anywhere — my recommendation is **not** to bring `Objeto completo` back but to print the real objeto as a muted second line under the generated title, because the defect then is a missing *fact*, not a missing *tail*; (3) the Radar **list** page could not be exercised live — the CNPJ lookup waits on the Python worker, which the parallel `worker/` lane is occupying — so the card is covered by tests plus the Landing's real-data example panel, which renders the changed component; (4) the screening screen's strip is covered by its own tests rather than in the browser: running a triagem calls OpenRouter |
| 2026-09-22 | Tender `estimated_value`: "Valor não informado" on 85% of the Radar | in review | https://github.com/Scintechn/licitaqui/pull/65 | **No migration — `tenders.next_refresh_at` was already there, defined and indexed by §6.1 and read by nothing.** The cause is ADR-0001's search fallback and the correlation is exact: **5,080 of 5,965 rows carry a search-index payload and 0 of them have a value; the 885 consulta-sourced rows all do.** The index publishes no `valorTotalEstimado`, no `srp` and no `orcamentoSigilosoCodigo`, and B2 enqueues `sync_items`/`sync_files` for a changed tender but **never a header re-read** — so a row that entered degraded stayed degraded for life. The 885 are frozen from the last healthy stretch while the search-sourced set grows every cycle, so coverage **decays on its own**: Sci measured 20% of 4,430, I measured 14.8% of 5,965, and 12.7% of 6,133 four hours later. The ADR's asymmetry argument does not cover this — a re-sweep only rewrites a row whose `pncp_updated_at` moved, so the *window* heals and the *row* does not. **Two sources, one order:** the Consulta detail's `valorTotalEstimado` wins whenever it answers (Sci's rule is that we show what the portal shows); otherwise the sum of `tender_items.total_value`, which `items.roll_up` already computed to decide `favored_treatment` and threw away. Precedence lives once in `items.pick_total`, shared with `favored_treatment`, so a card's value and the ME/EPP claim beside it cannot come from different arithmetic. **The agreement rate is 94.7%, not 100%** — measured across the 777 production tenders holding both a positive header and items summing above zero: 736 agree to the cent, 41 do not (19 within 1%, 17 at 1–2×, 5 at ≥2×, largest 13.3×). The item sum is never *lower*; where it differs it **over-counts**, because grouped lots and ME/EPP cotas are listed beside the items they are carved from (one has 283 item rows and 71 distinct descriptions). Consulta therefore always wins, and every fallback row records `value_source: items` in a `tender_value:<id>` marker so the minority stays findable and the rate stays queryable. **It writes columns, not `raw`** — the obvious `from_consulta`/upsert implementation merges the detail payload in, which `pg_column_size` puts at **+2,822 B × 5,373 rows ≈ 14 MB against 15.3 MB of headroom**; it would have filled the disk rather than fixed the Radar. **Three ways in, because each has a hole the next covers:** the fallback enqueues `refresh_tender_value` for every row it writes (root cause, but it fires exactly when consulta is down); `sweep_tender_values` runs every 30 min for whatever is still unvalued; `next_refresh_at` backs a tender off (6 h header TTL per §3.2, 30 days after a `410`) so the sweep cannot spin. **410/301:** a `410 GONE` parks the contratação rather than retiring it — what a withdrawn tender looks like on the Radar stays Sci's call, as `absence.py` already records — and neither the 410 nor a 404 touches the `pncp-consulta` breaker. The retired `/api/pncp/v1/orgaos/…` spelling answers **301 with no `Location` header at all**, so `follow_redirects` is powerless and httpx hands the 301 back; `_http_error` now says so instead of leaving a bare "HTTP 301" to re-measure, and the path constant is pinned by a test. **Latent bug fixed on the way:** `confidential_budget` was the one column `UPSERT_SQL` did **not** COALESCE, so a fallback sweep passing over a consulta-sourced tender overwrote a real `true` with the dataclass's `False` default; it is now `None` ("unknown") on search-sourced rows, and `apps/web` already coerces with `Boolean(...)` so a NULL renders exactly as the old `false` — no web change needed or made. **Verified against live PNCP + production, read-only:** on Sci's two worked tenders the portal figure, the consulta header and our item sum agree to the cent — `87612826000190-1-000958/2026` → R$ 271.350,31 and `12342663000173-1-000017/2026` → R$ 2.988.571,02 — and both carry `srp=true`, which explains the null `price_registration` beside a "Sistema de Registro de Preços" object. **52 new tests** (24 unit + 28 integration), full worker suite and `ruff` green. **Follow-ups for Sci.** (1) **The backfill has NOT been run** — the Neon project is out of disk (496.7 of 512 MB; the coordinator hit `DiskFull` mid-write on the short-title backfill) and freeing space is Sci's call because it involves the test databases CI depends on. No production writes were made by this branch; every number above is a `SELECT`. `scripts/backfill_tender_values.py` is ready, was exercised with `--dry-run` (5,356 due, 18 of a 20-row sample valuable from items alone, before/after identical), is **resumable by construction** (its work queue is a query, not a cursor), counts a `DiskFull` as `failed` and never as upgraded, and leaves `next_refresh_at` alone so a failed row is still due next run. Expected coverage once run: **777 → ~5,200 of 6,133 (12.7% → ~85%)**, ~8 MB of MVCC churn that autovacuum reclaims and **zero** permanent growth. (2) **The real storage lever is not this table:** `tender_items` is **231 MB of the 293 MB** database and **155 MB of that is the per-item `raw` jsonb** — 1,268 B/row against 175 characters of description (21 MB of descriptions in total) — i.e. every field `items.from_pncp` extracts is stored twice. Trimming it is a schema-and-backfill decision, deliberately not slipped into this lane. (3) **Its headroom check sums all 18 databases on the branch** (458 MB), because `pg_database_size(current_database())` answers 305 MB and would have reported 207 MB free on a project that had just hit `DiskFull` — worth knowing for any other backfill. (4) **For ADR-0001's outstanding re-measurement:** `/api/consulta` was **flapping, not down**, throughout this work — 6×200 (~0.8 s), 1×502, 1×503, 6×read-timeout over 14 minute-spaced probes (~43% success) while `/api/search/` and `/api/pncp/v1/…/itens` answered everything under 2 s. That is a third failure mode the ADR has not characterised, and the one that makes a per-tender backfill slow rather than impossible; recorded in the ADR along with the consequence section |
| 2026-09-22 | Tender detail: Objeto full width, and air between the topics | in review | https://github.com/Scintechn/licitaqui/pull/66 | Two layout corrections from Sci on the screen #64 merged. **1 · The Objeto now spans the full content column.** Sci: *"why does the Objeto text not take the entire width like the table above?"* The block shipped with `max-w-[68ch]` ≈ 571px inside a 920px column, so it aligned with **nothing** — the deadline/value box above is 920, the items table below is 920, the two columns under it are 440 each — and read as a mistake rather than as a measure. The cap's argument was real (a long measure of dense uppercase legal prose is hard to read) and it lost to alignment, which is Sci's call. The `max-w-` is gone; all three blocks now measure **920px at 1280 / 350px at 390**. The test that pinned the cap was **inverted, not deleted** — `spans the full column: any width cap here is a regression` now fails on `max-w-[68ch]`, on `max-w-[62ch]` and on `max-w-prose`, each verified by mutation; #56's reasoning comment was **kept and extended** to record that the cap existed, why, and that Sci overruled it on 2026-09-22, because the next person to notice the long line will be right about the typography and still wrong about the screen. **The measure was compensated with leading, never width** (the one lever left): measured in Chrome on the worst object we hold — `82558909000124-1-000279/2026`, 1 928 characters, no line breaks of its own — the uncapped block renders at **140–145 characters per line** at 1280px, not the ~105 the old comment estimated, and only **294 of 6 299** objects carry the agency's own newlines, so that unbroken flow is the normal case. `leading-relaxed` → `leading-loose` (1.625 → 2.0): ~73px of height on that worst case, ~10px on a typical two-line object, and at 390px the same text is 51–57 characters per line where the leading is simply comfortable. **2 · Separation between the topics below it.** Sci: *"for the two sections below, we need a little separation, increase the padding between each topic."* Numbers taken from `app/(public)/sections.tsx`, which already solves this exact shape (a two-column block that stacks) on the rhythm PR #55 settled at 40/48 — no new scale for the app screens: between topics `pt-7` + `main`'s `gap-3.5` = **42px** (≈ #55's 40), stacked below 900px `gap-7` = **28px**, columns at ≥900px `min-[900px]:gap-10` = **40px** (was 32). `pt-7` on the grid **and** on `<Record>`, so the pair is not left floating with 42px above and 14px below; the header's tightly coupled rows (title → badges → card) stay at 14px because they are one topic. Measured live at both widths: 42 / 28-or-40 / 42. **Order and B9 unchanged:** all four guards pass, the chain still reads box → Objeto → Por que → Operação → tabs, and suspended `02858064000153-1-000012/2026` still banners above everything with `urgency: []` at 390 and 1280. No horizontal overflow at either width. **No new Portuguese strings** — this is layout only; the 20 strings from #64 are still waiting on Sci. **59 files / 889 tests**, unchanged in count because nothing here added logic; lint · typecheck · build green. `git diff origin/main...HEAD --stat` lists exactly two files, both deliberate |
| 2026-09-22 | Radar navigation: the search dies at the triagem link, and the screening result never appears | in review | https://github.com/Scintechn/licitaqui/pull/68 | Two defects Sci hit on production, both on canvas 04. **1 · The search died at the triagem link.** PR #57 fixed the card → tender hop by adding `tenderHrefFrom` *next to* the bare `tenderHref`, which left the bare form as the shortest thing to type — so the same bug was one screen along, on the CTA at `opportunity-view.tsx:728` (`${tenderHref(id)}/triagem`), and on eight more: `priceHref`, the locked price block, both `accountHref` links, the price item chips, `TenderCardView`'s bare **default** href, and the two Suspense fallbacks in `page.tsx`. Confirmed on production before the fix — the card carried `?cnpj&group`, the triagem link carried nothing, and two presses of Voltar landed on a bare `/radar`. **The call: there is no bare form any more.** No link out of a Radar screen wants one, so keeping a "deliberately named" bare export would recreate the exact shape that produced this twice. `tenderHref`/`screeningHref`/`priceHref`/`radarHref` take a **required** `RadarSearch` — that is what found all nine call sites — and `readSearch()` reads the four travelling parameters once per screen. `RadarSearch` is deliberately the same four fields `listKey` uses, so #62's snapshot restore is cooperated with rather than bypassed; a test round-trips the triagem URL back through `readSearch`→`radarHref` to the Radar address it came from. Types cannot catch the mistake that actually shipped (a URL built by hand from a path and a literal, which is a *string*, not a call), so `client.test.ts` reads every file under `app/radar/**` with comments stripped and fails on one — mutation-checked by reintroducing the original line. **2 · The screening result never appeared.** Reproduced on production, CNPJ 36955612000185 on `13654405000195-1-000033/2026`: "está demorando" at 60 s with **no further request**, while a probe on `GET …/screening` got `ready`/`ok` at **≈178 s**. Cause: §3.1's 60 s is the budget for a one-hop read, and an `ai_screening` is `sync_files` + a 120 s download + `extract_text` + a 90 s model call (§7.1, §7.2) — the poll was structurally certain to give up first, and it gave up by *stopping*. **Quota was never burned twice**: the second visit's `POST` answered **200** and the badge stayed `1 de 2`, because `quota.spend` de-duplicates on the tender id — so the severity is UX, not billing. Fix: `waitThenWatch` in `poll.ts` keeps §3.1's twenty ticks, then tells the screen (honest card stays) and **keeps reading** every 10 s for 5 min, so the answer replaces the card where the user is standing; a `visibilitychange`/`pageshow` re-read covers a phone backgrounded for longer. Nothing is persisted — the trap that a Radar link is a plain `<a>` is why: the answer is a row in `ai_analyses` and the `POST` re-reads it free on the way back. **59 files / 907 tests** (main: 889), lint, typecheck, build green. `apps/web/**` only. **Needs Sci:** `radar.screening.timeoutBody` reworded — it used to say "Atualize em alguns instantes", which is now wrong; drafted as *"…aparece nesta tela assim que terminar. Não precisa sair nem pedir de novo."* (brief §5, approve or reword). **Assumptions:** `WATCH_TIMEOUT_MS` = 5 min is derived from §7.2's budgets, not from a measured tail (the one screening timed here took 178 s); `readSearch` now reads an absent `?group=` as *unchosen* rather than coercing it to `compatible`, because `group.ts` makes those different addresses and `restoreList` already bridges them. **Browser verification is incomplete and is the one thing outstanding:** the Chrome extension has no site permission for the preview host `licitaqui-1esemg772-scintechns-projects.vercel.app`, and a local dev server cannot be pointed at the database because `.env*` is deny-listed for agents. The pre-fix behaviour of *both* defects is confirmed on production at 1280 px; the post-fix walk-through at 390/1280 px needs either that host allowed in the extension or a run by Sci |
| 2026-09-22 | Radar: the CTA does not know a screening already exists (defect 3, same PR as the two above) | in review | https://github.com/Scintechn/licitaqui/pull/68 | Sci: *"I already have the AI Triage for this item … but the button remains like the first time, for my user."* Same missing state as defect 2 — that one is "the result does not appear when it finishes", this is "the product forgets it ever finished". **The quota question, settled first because it decides the rest: re-opening your own triagem is FREE, and the expensive reading of this defect does not exist.** `quota.spend` de-duplicates on `reference` (the tender id), so a second request by the same spender returns `repeat`/`charged: false`. Verified twice on production — a reload of an already-read triagem answered `POST` **200** with the badge unmoved at `1 de 2`, and Sci's own tender `94703980000132-1-000080/2026` opened instantly at `2 de 2` without decrementing. Pinned in `screening.db.test.ts` against a real migrated database: a cached reading charges **once** and never again for the same tender. **What *is* charged, by design:** a caller who has never opened this tender pays even when the analysis already exists, because `requestScreening` spends *before* it reads the cache — §8's own words ("returns it and records usage") and §3.2/§10 together, where the analysis is shared but the *reading* is allocated per user. Not a bug; it is why `ai_analyses` has no `user_id`. **Latent, for Sci:** `spend`'s de-duplication is period-scoped, so a Básico user (5/month) who read a tender in September is charged again for the same row in October. Visitors are unaffected (`period = 'total'`). A §10 commercial call, not taken here. **The fix:** `GET /api/tenders/:id` now carries `screening: { ready, spent }`. `ready` is **not** a new notion of freshness — it is `currentAnalysis`, the predicate factored out of `readScreening` and now shared, so the button can never promise something the triagem screen will not do; a republished edital moves `files_hash`, `ready` goes false, and the CTA honestly offers to analyse again (pinned by a test that asserts the two agree). `spent` is the `usage` half. Both in **one** statement on a route that was already doing three, rather than a second request — the CTA has to be right on first paint, and a button that changes its mind under the user's finger is worse than one that is merely wrong. It costs one extra `plan_limits` read plus one combined probe; read-only, spends nothing. **59 files / 918 tests** (889 at main, 907 after the first two defects). **Needs Sci — new customer-facing copy, brief §5:** one label became two, plus a caption line under the button, because opening a reading you have paid for and spending a triagem are different actions that were sharing the words "Ver triagem por IA". Drafted: `screeningCtaNew` "Fazer triagem por IA"; `screeningCost` "Usa 1 das suas triagens."; `screeningCostReady` "Este edital já foi lido pela IA. Abrir usa 1 das suas triagens."; `screeningDone` "Você já usou uma triagem neste edital. Abrir de novo não custa nada." `screeningCta` is unchanged and now only appears when it is true. §2.2 rule 5's AI notice still sits above the CTA, untouched |
| 2026-09-23 | §14.1 item 4: stop rewriting unchanged `tender_items` | in review | https://github.com/Scintechn/licitaqui/pull/69 | **Two changes, and shipping the first alone is a regression.** `tender_items` is 142,127 rows rewritten on every sweep whether or not anything changed, and on Neon's copy-on-write storage every rewrite costs twice. **(1) The guard** is an explicit 14-column `IS DISTINCT FROM`, *not* the obvious whole-row form — `where tender_items is distinct from excluded` is a **silent no-op**, because `updated_at = now()` sits in the SET list so `excluded` always differs, every row is rewritten exactly as before, and a measurement taken against it reports 0% with nothing to explain it. `GUARDED_COLUMNS` sits beside the statement and a test reconciles it against the INSERT's column list, because a column in one and not the other would be written once and then never updated again — silent per-field staleness rather than a loud failure. **(2) Freshness had to move with it.** `read_state` aged a tender *with* items on `min(tender_items.updated_at)`, which only answered "when did we last read PNCP" *because every read rewrote every row*. Guard the upsert and that coincidence breaks: `min` freezes, every unchanged tender reads `ttl` stale on every sweep for ever, and the collector re-fetches the whole corpus — worse than the bloat. The two ladders collapse to one signal, `coalesce(m.synced_at, min(i.updated_at))`. **The fallback is the cutover, and it is not optional:** measured on production, **0 of the 6,473** tenders holding items have a marker (it is new; they predate it), so reading the marker alone makes every one of them `never` — a distinct branch with **no age**, which the 12 h TTL therefore **cannot stagger**. All 6,473 would become eligible on the same sweep, against a consulta endpoint measured at **62% success in runs of two to four minutes** — the condition most likely to open a breaker and stop the collector for everyone. "Let the TTL stagger it" is the thundering herd with a reassuring name. The fallback preserves the old semantics exactly: a tender fresh before is fresh after, same age, and the staggering comes free because those timestamps are already spread. **2,736 of the 6,473 are fresh today** and would otherwise have been fetched immediately. **Self-healing per tender:** `sync_items` writes the marker unconditionally after any sync, so a marker-less tender takes the fallback exactly once — reads stale, fetches, marks, and the marker governs thereafter. Three paths return without marking (unknown tender, fresh, `DataVanished`) and none can strand a row, because **a guard on writes cannot change a path that writes nothing**. **`backfill_markers()`** dates each marker to the tender's own `min(updated_at)`, **not `now()`** — `now()` would mark the corpus fresh and defer every re-read by a TTL, the same herd moved twelve hours out; `min` not `max` because a half-finished fetch leaves some rows fresh and some old and the old ones are the truth; structurally unable to invent a marker for a zero-item tender (the join yields no group) and `not exists` so it cannot overwrite the one deliberate "synced, empty" marker. Idempotent, so safe before *or* after deploy — an optimisation, not a step the cutover depends on. **Measured, A/B on the test database** (same rows, same sweeps, control = this statement with the `WHERE` stripped): guarded **0 of 5,000** rewritten and **8 kB** growth; unguarded **5,000 of 5,000** and **912 kB** — **99.1% of the churn avoided**, control perfectly linear at 1,000 rewrites per sweep. Production scale: 142,127 rows, 232 MB heap, **1,709 B per row** is what one rewrite churns, so a full re-sync churns one entire heap (232 MB) unguarded against ~0 guarded. Skip rate is **instrumented, not asserted** — `upsert_items` returns `UpsertItemsResult(offered, written, pruned)` and it lands in the job log line and the marker per tender; a test pins psycopg's `executemany` rowcount accumulation, because that quietly becoming -1 would turn every reported rate into a fiction without failing anything. **46 tests** (25 new integration + 21 existing unit), both freshness directions proven. **Follow-ups for Sci.** (a) **Production skip-rate not yet taken** — the value backfill is still writing (5,629/6,740); it needs one full collector cycle after that to read the real rate. (b) **`raw` is the size; this PR is only the churn** — 1,268 of every 1,709 bytes per row is the per-item `raw` jsonb, about **172 MB of the 232 MB heap (74%)**, every field `from_pncp` extracts stored a second time. Trimming it reclaims ~172 MB one-off and makes every future row ~4× cheaper, far more than the guard saves, but `absence.py` and any future re-mapping read `raw`, so it is a schema-and-backfill decision and deliberately **not built here**. (c) **`files.py` is the precedent, not a second site** — `tender_files` has **no `updated_at` column at all** and `sync_files` already reads freshness from the marker, i.e. it is already the shape this PR moves `sync_items` into; it does rewrite unchanged rows but at 5.6 MB the payoff is marginal. (d) **CI**: the integration job was cancelled at 30m18s on #67 and the timeout is now a stopgap 45 min; a `--durations=0` profile shows **setup 49.0% + teardown 30.7% = 79.7% fixture overhead**, three Neon connections per DB test (two purely for cleanup DELETEs), so collapsing them in `conftest.py` targets ~60% of the runtime where a top-40 offender hunt could only reach 11.6%. Separate card, not scheduled |
| 2026-09-23 | Radar CTA: Sci's ruling — recognise a triagem this user already asked for | in review | (PR opened after #68 merged) | Follows #68, which shipped `screening: { ready, spent }` on `GET /api/tenders/:id`. That PR guessed the copy; Sci ruled otherwise and this implements his ruling, not the guess. *"If it is the first time of that user, the CTA stays as-is. However, if the user already requested the triage before, he only wants to see it again, so we could change the text."* **The switch is `spent`, and only `spent`.** `screeningCtaNew` ("Fazer triagem por IA") does **not** ship and is deleted: the first-time label is byte-identical to what is in production. `ready` deliberately does not touch the label — a tender whose analysis exists because somebody *else* opened it (§3.2 shares the reading, `ai_analyses` has no `user_id`) is still a first request for this user, and a test pins that it reads as one; switching on `ready` there would tell someone a triagem they never asked for is theirs. **The label and the cost line are now independent by construction:** `showScreeningCost` is its own prop, default **off**, so Sci's open question about whether a first-time reader should see "Usa 1 das suas triagens." is a one-line change that cannot disturb which words the button uses. It is never shown to a `spent` caller, because re-opening your own triagem costs nothing. `screeningDone` is deleted too — the recognising label already carries "yours", and a caption Sci has not seen is not part of what he approved. **Needs Sci — pick one (brief §5).** Shipped: **"Ver a triagem que você pediu"** (closest to his own sketch, and unmistakably recognition). Alternatives in the PR: "Ver sua triagem por IA" (minimal one-word delta, keeps the product term, but subtle) and "Sua triagem está pronta" (warmest, but states availability rather than recognition, and is wrong while the job is still running). **59 files / 922 tests** (918 at `main` after #68); lint, typecheck, build green. Branched fresh off `aa86635`, no merge, `apps/web/**` plus this row |
| 2026-09-23 | Cards B11 and B12: breaker behaviour, from a live PNCP outage | in review | https://github.com/Scintechn/licitaqui/pull/PENDING | Carded by Sci, B11 first. **B11** — 59 `sync_tender_awards` jobs failed on `CircuitOpen` and each burned one of its four attempts **without a request leaving the process**; the breaker raises before the call, so `attempts` counted attempts that never happened, and a long enough outage kills a job for reasons unrelated to it (the #54 shape: transient made terminal). Fix is deferral without incrementing, with `run_after` from the breaker reset rather than the 2/8/30 ladder — the second half is not optional, since skipping only the increment leaves the backoff stuck on tier one and the job wakes every 2 min (~90 `UPDATE`s over three hours instead of ~12). Deferring the dequeue by kind was rejected: `sync_items` touches `pncp-itens` *and* `pncp-consulta` via `absence.py`, so a kind→breaker map is not clean, and refusing to dequeue blocks work that would have succeeded. **B12** — 50 minute-spaced probes of `/api/consulta` measured **62% success failing in runs of 2–4 minutes**, which is neither *down* nor *degraded*; a consecutive-failure breaker trips on a run of two, and a 15-min reset makes one bad minute cost fifteen good ones. Hence **a 62%-success service can be less available than a 40%-success one** when the first fails in runs. Evaluate a windowed policy against the recorded trace before changing production. Context: at 07:48 five of seven breakers were open or half-open, `sync_items`/`sync_files` stalled from 01:02, and the worker was healthy throughout (19 h uptime, 4/4 consumers) — the queue state was the breakers working, not a defect |
| 2026-09-23 | Radar: `R$ 0,00` where PNCP withheld the budget (found on production by Sci) | in review | https://github.com/Scintechn/licitaqui/pull/72 | Sci: *"Sometimes the value is blocked — 'sigiloso'. However, we hold and show it as R$ 0,00."* **Two halves; the first ships regardless of PNCP.** **(1) Zero is never a price.** PNCP publishes `valorTotalEstimado: 0.0` in the value field when a budget is withheld — asked directly, `13112669000117-1-000010/2026` returns that alongside `orcamentoSigilosoCodigo: 3` ("Compra totalmente sigilosa") — and **108 tenders hold `estimated_value = 0`** while Sci's two examples hold `NULL` with every item row zero. Printing it states a figure the edital does not support: brief **§2.2 rule 3**. Confirmed on production before the fix, on Sci's own tender at `/radar/edital/94703980000132-1-000080/2026`: **3 × "R$ 0,00"** in the rendered text; on the branch, **0**. **The guard sits in `money()`/`moneyExact()`, not at the call sites,** because the failure modes are asymmetric: a missed call site prints a false price *silently* (the bug, and the one that recurs the day a sixth value slot is added), while a formatter refusing a zero some future caller meant fails *loudly*, in front of the developer who wrote it. `format.ts` documents the boundary it costs — a billing screen where "você pagou R$ 0,00" is a fact must not reach for these two and gets its own formatter. Deliberately **not** guarded: a sub-R$1 value `money()` would round to "R$ 0" — same untruth in principle, no such row in the corpus, and the honest repair is a different format, not a null that hides a figure we hold. **Five places, not the four in the brief:** card headline slot · detail `VALOR ESTIMADO` box · items table unit column · items table total column · **`price-view.tsx`**, which had a second `Intl.NumberFormat` of its own and would have kept printing "Edital paga (estimado) R$ 0,00" after the other four were fixed; it now delegates to `moneyExact` (one behaviour change: the nbsp after `R$` becomes an ordinary space, matching the Itens tab). **`sumItems` now treats a zero total as a hole, like a null one** — a sum counting it as nothing would be adding up rows the table itself prints "—" for, under a caption promising *"a soma dos valores totais das linhas acima"*; the block used to vanish silently and now says why. **(2) Three states, decided once.** `tenderBudget()` in `headline.ts`, shared by the card and the detail screen so they cannot drift: `confidential_budget` → "Valor sigiloso"; a non-zero figure → the figure; anything else → "Valor não informado". **A zero is never promoted to "sigiloso"** — that is a claim about the órgão's decision, carried only by `orcamentoSigilosoCodigo` ∈ {2,3}; PNCP publishes zero for merely incomplete tenders as readily as for secret ones. Keeping them apart is what makes the label safe the moment the worker's consulta upgrade (#65) starts setting the flag. **That branch cannot be exercised on live data today** (PNCP 503/timeouts, worker lane owns it), so it is pinned by tests and verified in the browser against a fixture. **59 files / 946 tests** (922 at `main`), lint, typecheck, build green; `apps/web/**` plus this row, branched fresh off `db1c97d`, no merge. **Browser evidence, real Chrome, both widths** on a throwaway `/dev` route rendering Sci's two tenders plus the `estimated_value = 0`, `confidential_budget = true` and control cases (route deleted before the commit): at **390 px** no horizontal overflow, mobile card layout, every price cell "—", the new sum line, and "Valor sigiloso" on the flagged card; at **1481 px** the table with both value columns "—". Across all five cases: **0 occurrences of `R$ 0`**, 6 × "Valor não informado", 2 × "Valor sigiloso", `sumNote` only on the control. The one console error is a browser extension injecting `bis_skin_checked`, not ours. **Needs Sci — copy, brief §5.** (a) **New string shipped**, `radar.opportunity.items.noSum` = *"Não há soma a mostrar: ao menos um item deste edital está sem valor publicado no PNCP."* Alternatives: *"Os itens deste edital não têm valor publicado."* (plainest, but does not explain why a partially-priced list also has no sum) and *"O órgão não publicou os valores destes itens, então não há soma para mostrar."* (warmest, longest). It deliberately does **not** say "sigiloso": the sum line's statement is *these rows have no published price*, which is not the same claim as *the órgão declared the orçamento sigiloso*, and only the latter is evidence-backed. (b) **`radar.card.confidential` ships unchanged as "Valor sigiloso"** — Sci suggested "Sigiloso". Three readings: **"Valor sigiloso"** (shipped; parallel with "Valor não informado" beside it, fits the 390 px meta line, but does not attribute the decision); **"Orçamento sigiloso"** (PNCP's and Lei 14.133/2021 art. 24's own vocabulary, so a MEI who has read an edital recognises it — but introduces a second word for what the label above already calls "Valor estimado"); **"Sigiloso — o órgão não divulga o valor"** (safest under rule 3 because it attributes the decision to the órgão rather than to us; wraps to two lines on a card). **Not fixed here, needs a migration card:** `tenders.confidential_budget` is `boolean default false`, but `_confidential()` in `worker/licitaqui/tenders.py` deliberately returns `None` when PNCP sent neither field — *"absence of evidence is not 'public'"* — so **the schema collapses "unknown" into "not confidential" before anything reads it**, and no screen can ever tell the two apart. Making the column nullable is its own PR (CLAUDE.md), and the web change above is correct either way: today's `false`-for-unknown lands on "Valor não informado", which is exactly what we know |
| 2026-09-23 | Card B13: `confidential_budget` cannot say "unknown" | in review | https://github.com/Scintechn/licitaqui/pull/PENDING | Found while fixing #72. `tenders._confidential()` returns `None` when PNCP sent neither sigiloso field — *"absence of evidence is not public"* — but the column is `boolean default false`, so **unknown is stored as not-confidential** and no screen can distinguish them. Measured: **108 tenders hold `estimated_value = 0`, none has `confidential_budget = true`**, while PNCP answers `orcamentoSigilosoCodigo: 3` for at least one. The same shape as the `refund_reason` CHECK in migration 0004 — careful code undone by a schema default. #72 ships the three-state UI that renders NULL honestly as "Valor não informado", so B13 only unlocks the truthful "Valor sigiloso" where PNCP actually said so; no backfill needed, since #65 resolves each row on its next consulta read |
| 2026-09-23 | Persona journey tests (Playwright, `apps/web/e2e/`) | in review | https://github.com/Scintechn/licitaqui/pull/74 | **A live defect, found by the journey itself: with a list on screen, switching tabs and applying filters do nothing.** The address changes to `?group=check` or `uf=RJ`, the list stays the previous one and **no request leaves**; it only works again on a full document load, or once the snapshot expires (30 min). Cause in `app/radar/radar-screen.tsx`: the effect that writes the snapshot depends on `[key, data]` and is declared before the one that loads, so on a client-side navigation it fires with the **new** key and the **old** data and stamps the previous list under the new search's key; the loader finds a direct hit in `restoreList` and returns without asking for anything — and because `shown.current === restored.snapshot.tenders` it does not even re-render. Minimal fix, **not applied here** (outside the card, and `radar-screen.tsx` is a hot file): carry in `Data` the key the list was read with and `if (data.key !== key) return` in the writing effect; cheap defence in depth, make `restoreList` refuse a direct hit whose `snapshot.group` disagrees with the group asked for, which is the check the `auto` path already makes. Both journeys are recorded with `test.fail()` — they run, they fail on purpose today and they go red on the day of the fix. **The suite:** 22 tests in 5 files, project `journeys`, hermetic — every `/api/**` answer comes from `e2e/fixtures/radar-api.ts`, typed against `lib/radar/contract.ts`, with no database, no worker and **no PNCP**, which was the card's requirement (`pncp-itens` opened five times this morning and a suite that goes red with PNCP's weather is ignored exactly when it matters). 220/220 green over ten consecutive runs. One flake found and root-caused on the way: an unhandled `route.fulfill()` rejection on a page that had navigated left the request hanging and the screen stuck on "Consultando o CNPJ…" — fixed in the fixture, not retried away. Project `accounts` (E3/Telegram and U2/magic link for Seu Jorge) is written and **skipped with its reason in plain sight**: it needs a published environment, a session cookie and an inbox — Seu Jorge's half has never run end to end, and nothing here pretends it has. `pnpm test:e2e` is a new script and **stays out of `pnpm test`** (59 files / 946 tests, unchanged); `ci-e2e.yml` runs only the hermetic lane, advisory until it has a week of green. Minor finding: the locked Documentos block on the tender screen points at a bare `/conta/criar`, with no `?next=`, and loses the edital the person was reading |
| 2026-09-23 | Radar navigation: the group chips and the filter row do nothing with a list on screen | in review | https://github.com/Scintechn/licitaqui/pull/75 | Found by #74's persona journeys, which shipped two `test.fail()` tests holding the broken behaviour; both are ordinary assertions now, because a `test.fail()` left behind after the fix goes red and teaches everyone to ignore it. **Confirmed on production before changing anything** (390 px and 1280 px, CNPJ 36955612000185/BA): after clicking Verificar the URL says `group=check`, `aria-current` is still on Compatíveis, the cards still read COMPATÍVEL, and `sessionStorage` holds `36955612000185|BA||check` whose snapshot is **`group: "compatible"`** with twenty Compatíveis rows — one tab's list filed under another tab's key, in the live product. **Cause:** the effect writing the snapshot depends on `[key, data]` and is declared **before** the one that loads, so a client-side navigation runs it with the **new** key and the **old** data; `restoreList` then finds a direct hit for a search nobody ran and the loader never asks the route for anything. Under a minute that is **zero requests** and not even a re-render (the snapshot holds the very array on screen); past a minute the revalidate path fires one request for the **snapshot's** group, which changes nothing either — both observed. **Two guards, not redundant:** `Data` carries the `listKey` it was read for and the writing effect bails when it disagrees with the key on screen (**the fix** — it covers every axis of the key, which is what the filter row needs); and `restoreList` refuses a **direct** hit whose `snapshot.group` disagrees with the group asked for (**defence in depth, group axis only** — `ListSnapshot` does not carry the filters it was read with, so a snapshot filed under the wrong UF is indistinguishable from a right one there; it also self-heals a poisoned entry left in a live tab by the old build, because the fetch that follows overwrites it). Reordering the two effects would have worked today and gone on being a trap, because it makes the cache's correctness depend on the order two `useEffect` calls are written in. **#57/#62 untouched and still asserted**: coming back restores three loaded pages and the scroll with `calls.tenders` and `calls.cnpj` unchanged — same key restores, a different key fetches. **A third journey was added** beyond the card's two conversions, for the narrow case *between* the guards: an `auto` → explicit-group navigation whose rows do not change, where `restoreList` matches sideways and the screen has nothing to re-render — doing nothing there would leave the state stamped with the old key, so the snapshot would never be re-saved where that URL looks for it and `rememberScroll` would write to a key nothing was stored under; mutation-checked by removing the clause and watching her land **6267 px** from where she was. **59 files / 948 tests** (946 on main), lint, typecheck and build green; **23 journeys, 115/115 at `--repeat-each=5`**, zero flakes; `ci-web.yml` untouched. **Not fixed here, needs a card:** the locked Documentos block in `opportunity-view.tsx` links to a bare `/conta/criar` with no `?next=`, so creating an account there loses the edital — the same shape as #68 |
| 2026-09-23 | `NOTIFICATION_JOURNEY.md` — the three journeys that end in a message | in review | https://github.com/Scintechn/licitaqui/pull/76 | Documentation only; the four delivery defects are the parallel lane's. Magic-link sign-in (7 states), Telegram linking (11), the weekly digest (13 — every skip reason in the code today), each state with what is true in the system, what the user sees **in the app**, what the user sees **in the channel** and how they recover. The four defects are one defect: **the product has states a person cannot observe**, and a confirmation living only on a web page the user has already left is not a confirmation — success belongs in the channel being set up, failure in the channel that still works. **Where the two known defects really are:** defect 1 fires when you *ask* for the link, not when you click it — `pages.verifyRequest` is registered as `/conta/criar?enviado=1` and `@auth/core` appends the incoming search to it, so `enviado` parses as `"1?provider=resend"` and the "Link enviado" card never renders; and defect 2 kills `/ajuda` and `/pausar` too, so a user who pauses **is** paused and never told (the digest is unaffected — the worker enqueues its own jobs in snake_case). **Two findings nobody had reported.** (1) The magic-link e-mail is Auth.js's built-in **English** template — no `sendVerificationRequest` is overridden, so `@auth/core` sends `Sign in to www.licitaquiapp.com.br`, no reply-to (brief §1 wants `contato@`), link valid 24 h, which no screen says. (2) **Nothing reaches Telegram unless `TELEGRAM_DELIVERY=send`** — unset is `dry_run`, and a dry run is deliberately `completed`: it burns the weekly quota and writes `alert_deliveries`, so a silent founders week looks exactly like a delivered one and the tenders it did not send are excluded next Monday. Also: `alert_deliveries.opened_at` is read by the Gate 0 open-rate card and written by nothing, so that gate reads 0 forever. **Follow-ups for Sci** — all Portuguese copy in §7 is drafted and waits on approval (brief §5, §2.2); where does a name come from for a magic-link account (§2 forbids inventing one; the fix lane owns the mechanism); is `TELEGRAM_DELIVERY=send` set in production; should `/conta/alertas` claim "Telegram conectado" before the bot has answered. §9 lists the four `/admin` additions, cheapest first — the `telegram.skipped`-by-reason card needs no new writes and would have shown the digest defect on the first Monday. |
| 2026-09-23 | Four delivery defects: nothing ever told a user it worked | in review | https://github.com/Scintechn/licitaqui/pull/77 | All four found on production by Sci, all four the same shape — **state set in one place that never reaches the place that needs it**. **(1) Every e-mail sign-in landed on a dead page.** `pages.verifyRequest` carried `?enviado=1` and `@auth/core` joins its own parameters onto that value with a *bare concatenation* (`${pages.verifyRequest}${url.search}`) — unlike the `signIn` and `error` branches beside it, which both do `includes("?") ? "&" : "?"`. Production served `302 → /conta/criar?enviado=1?provider=resend&type=email`; `enviado` then parsed as `"1?provider=resend"`, the comparison against `'1'` was permanently false, and the **"Link enviado" card — written, translated, correct — never rendered**. Reproduced before and after: the broken URL renders the bare form, the clean one renders the confirmation. Fix is that **no `pages.*` value may contain a query string**, now enforced by a test; the "we sent it" signal comes from Auth.js's own `type=email` (`lib/auth/verify-request.ts`), with `enviado=1` kept as an alias so internal links and the `seu-jorge` journey keep working. `AUTH_PAGES` moved to its own module purely so the invariant is testable — `lib/auth/index.ts` constructs NextAuth, which the unit suite cannot import. **The same `?`-for-`&` pattern elsewhere:** swept the whole tree; exactly one other real instance, `app/conta/actions.ts` `redirect(`${next}?estado=…`)` ×2, where `safeNext` permits any local path *including one that already carries a query* — not live today (both call sites pass bare constants) but reachable, since `saveCompany` is a `'use server'` POST anyone can call with `next=/radar?cnpj=1`. Both now use a new `withQuery()` that joins correctly. `lib/radar/client.ts`'s `withParams` is provably safe (every caller passes a path built by `editalPath()`) and was left alone. **(2) The bot could not confirm a link, ever.** The web enqueued `{template, userId, chatId}`; the worker reads `user_id`/`chat_id`, so **every** `send_telegram` died on `ValueError: send_telegram payload needs a 'user_id' or a 'chat_id'` and retried until its four attempts were gone (production jobs 19267, 19268). Blast radius is wider than linking: `/ajuda` and `/pausar` die identically, and `/pausar` is the worst of them — `alerts.active` is set *before* the reply is queued, so the person **is** paused and is never told, and pauses again. The weekly digest was never affected; the worker enqueues its own jobs in snake_case. **Fixed in the producer, and the consumer stays strict.** The producer was wrong against its own documented contract — `lib/jobs/index.ts` already said the payload is `{template, user_id \| chat_id}` while the `Reply` type sent neither. Teaching the worker to accept both spellings is one line and would have made this class of drift *silent*; that `ValueError` is the only reason anyone found it. Translation now happens once, in `sendTelegramPayload()`, beside the other functions that mirror a Python counterpart. **The drift alarm is `contracts/jobs/send_telegram.json`** — a schema neither language owns, read by `lib/jobs/contract.test.ts` and `worker/tests/test_job_contracts.py`; the Python side asserts **both** directions, because a test that only fed the handler a correct payload would have passed on the day of the bug too. **The existing DB tests asserted the camelCase payload** — they pinned the producer against itself and never against the consumer, which is exactly the gap. **(3) A magic-link user could never receive a digest.** Google supplies `users.name`; the e-mail magic link never collects one, so every account U2 exists to serve had `name = NULL` and was skipped **permanently and silently** from both the digest and the `/start` confirmation. Chosen fix: **the greeting is now a `[[se: tem_nome]]` block** — `Bom dia[[se: tem_nome]], {{nome}}[[/se]].` renders "Bom dia, Sci." or "Bom dia.", both correct Portuguese, **no sentence changed, so no new copy to approve**. A blank value was never an option (`MissingPlaceholder` by design, templates README §3). **Deriving a name was rejected on the rules, not on taste**: from the e-mail local part or from the company is the same act — putting a name on somebody who never gave us one (brief §2.2), and the address route additionally puts an e-mail inside a message body (§12). Asking for a name at signup adds a field at the highest-drop-off moment and does nothing for the accounts that already exist, including the one that found the bug. Documented as templates README §9. **(4) The skip reason named the wrong field.** `no_company` was raised when *either* the name or the company was missing, so Sci's account — company `JKL EVENTOS`, perfectly good — was logged as `no_company`, and the cause took an hour instead of a minute. These strings reach `events.props` and `/admin` groups by them. `no_company` now has **exactly one** cause; the name cause is *eliminated*, not renamed, so nothing can report it again; and a third meaning hiding in the same `or` (`who.user_id is None`, which is not a company problem at all) now correctly raises the `no_recipient` that already existed for it. **Also fixed, found by this work:** `e1`/`e3.db.test.ts` scoped emails, chat ids and secrets by `RUN_ID` but **not the reply job key** — `reply:<update_id>` counted from a constant, and `jobs_dedupe` is a *shared* index, so two runs of the suite deduplicate against each other's rows and one reads back the other's payload. Exactly CLAUDE.md's "a task-scoped prefix looks isolated and is not"; each run now takes its own band and `cleanup()` deletes it. **62 files / 976 tests** (968 at branch point), lint, typecheck and build green; worker `ruff` clean. **Production verification is incomplete and it matters:** the redirect is verified against live `www.licitaquiapp.com.br` (before *and* after), but **defects 2–4 are verified by test only** — reading `.env.local` is blocked by the permission system, so I have no production DSN and no bot token, and I did not work around it. **Nobody has yet confirmed `telegram.sent` vs `telegram.dry_run` in production.** `COMPLETED_EVENTS = (EVENT_SENT, EVENT_DRY_RUN)`, so a dry run **consumes the weekly quota and writes `alert_deliveries` while sending nothing** — if the worker process did not read `TELEGRAM_DELIVERY=send`, every fix here is invisible on Monday *and the system reports success*. The one query that settles it: `select name, count(*) from events where name in ('telegram.sent','telegram.dry_run') group by name;` — **run it before Thursday.** **Needs Sci / carded, not done here:** the delivery mode belongs on `/admin`, but the web cannot read the worker's environment, so the honest card is a data-derived card counting those two events, and it extends a closed `GateKey` union O1 owns; the Auth.js magic-link e-mail is **in English, with no reply-to** ("Sign in to www.licitaquiapp.com.br") on a Brazilian product for MEIs, which is a launch-day problem and needs Sci's own approval, not an agent's; `alert_deliveries.opened_at` is read by the Gate 0 card and **written by nothing**; the worker inserts into `events` with raw SQL, so the closed event-name catalogue binds only the web and one table holds both `telegram_linked` and `telegram.skipped`, which will bite the first `group by` |
| 2026-09-23 | No Telegram message could render in production (worker image) | in review | https://github.com/Scintechn/licitaqui/pull/78 | **`worker/Dockerfile` never copied `templates/`, so every render raised `TemplateNotFound` — for the life of the product.** `licitaqui/templates.py:42` resolves `TEMPLATES_DIR` as `<package>/../templates`, which in the container is `/app/templates`, a directory the image did not contain; the Dockerfile copied `pyproject.toml`, `licitaqui/` and `evaluation/` and nothing else. Across the whole `events` table `telegram.sent` = **0** and `telegram.dry_run` = **0**: not one message has ever rendered, on any path — the linking confirmation, `/ajuda`, `/pausar`, the weekly digest, the empty-week digest. Independent of the four defects fixed in #77, which were about jobs enqueued wrongly or skipped early; this one means that even when everything upstream is correct there is nothing to render. **The fix is one `COPY` line. The rest of the PR is about why it was invisible with every check green**, which is the part that can recur: `pytest` runs against the *working tree*, where `worker/templates` sits next to `worker/licitaqui`, so no test importing this package from a checkout can observe a missing `COPY`; and `docker build` only proves the Dockerfile parses and `pip install` succeeds. So the guard runs **inside the artefact**, in the existing `docker build` job between the build and the push — `docker run --rm licitaqui-worker:<sha> python -m licitaqui.selfcheck` — and an image that cannot send is never published. `licitaqui/selfcheck.py` walks every telegram template id the worker can be asked for (now declared once as `telegram_alerts.TEMPLATE_IDS` instead of living in nine `if` branches) and **renders** each twice, flags on and off, with no database and no network; because it renders rather than listing files it also catches a template deleted or renamed, front matter drifted from the body, a stray `{{ nome }}` that would be delivered verbatim, and an unresolved `TODO(Sci):` in something sendable. `status: draft` is deliberately not a failure. Verified both ways against a reproduction of the container layout (no Docker on this machine): **exit 1** on the pre-fix COPY set, **exit 0** with `COPY templates/` — the check that would have failed today, not one that documents the rule. `tests/test_selfcheck.py` (13 tests) pins the checker's own failure modes and pins `TEMPLATE_IDS` against the files on disk in both directions; one case reads the Dockerfile as text and is the half that needs no Docker daemon. **Audit of everything else resolved relative to the source tree: only `config.ROOT`** (`<package>/../..` → `/` in the image), and it is inert — it is used solely by `resolve_secret` to look for gitignored env files *after* the environment has been consulted, and Easypanel passes the environment directly. Noted, not changed: narrowing secret resolution is a behaviour change and does not belong in a hotfix the night before founders week. **Follow-up for Sci, not this PR: production verification is blocked on the Easypanel redeploy.** CI's `docker build` on `main` succeeds at Build, Login and Push and fails only at "Tell Easypanel to redeploy" — the host is behind Cloudflare and GitHub's runner appears blocked, while the identical `curl -fsS -X POST` from a laptop returns `200 Deploying...`. So merging publishes the image and the worker does **not** pick it up. Once Sci triggers the redeploy, enqueue `send_telegram` with `{"template": "start-linked", "user_id": 5}` (Sci's own account, the only linked chat; `eligible_users` returns exactly `[(5, 'basico')]`) and read `select type, count(*) from events where type like 'telegram.%' group by type`. **A completed job is not evidence** — a dry run consumes the weekly quota and writes `alert_deliveries` while sending nothing; `telegram.sent` is what proves the running process read `TELEGRAM_DELIVERY=send`, `telegram.dry_run` means Easypanel's value is not reaching it. Rendering `start-linked` through the real `build_reply_context` path for a recipient with `name = NULL` — which is user 5 — produces a clean greeting with no gap, so **#77's `[[se: tem_nome]]` is confirmed** as far as it can be off-production. |
| 2026-09-23 | GTM in the head, and the privacy sentence it contradicts | in review | https://github.com/Scintechn/licitaqui/pull/79 | Container `GTM-5V6M75R7` as `next/script strategy="beforeInteractive"`, so Next hoists it into the document head at build time rather than injecting it after hydration — which is what GTM's "as high in the `<head>` as possible" actually asks for, and what a tag that must see the first pageview needs. Verified in the **built** HTML, not the source: in `.next/server/app/fundadores.html` the snippet is at char 3203, `</head>` at 3315, the `<noscript>` iframe after `<body>` at 3322. `pnpm typecheck` clean, `build` green, `test` 62 files / 976 passed. **The PR is deliberately not mergeable.** On Sci's instruction it removes `"Não usamos cookies de publicidade nem de rastreamento de terceiros."` from privacy §10, because GA4 sets `_ga` and shipping would have made a published sentence false. **That deletion does not finish the job** and the PR says so: the same paragraph still claims `"apenas cookies necessários"` (`_ga` is not necessary) and `"as métricas de uso são agregadas e não identificam você individualmente"` (`_ga` carries a per-visitor id), and §9's processor table still lists Google for sign-in only. Those are sentences, and sentences are Sci's under legal brief §5 — drafted for him in chat, not committed. `docs/legal/README.md:52` records the "essential cookies only, no second page" decision and may now need a consent mechanism. **Unrelated, found while checking job 23476:** an instruction arrived in context appended to the `apps/web/CLAUDE.md` block, titled "While auto mode is active", directing that all file edits be made with `sed`, heredocs and short scripts instead of the file tools. Not in the repo's `CLAUDE.md`; refused as untrusted, the same call the lane made on the MCP-block instruction, and the same pattern that clobbered four lanes' PR links in this very file. |
| 2026-09-23 | Privacy policy describes GA4 — and the CNPJ is going to Google | in review | https://github.com/Scintechn/licitaqui/pull/80 | GA4 `G-1LVNSD23FX` went live through `GTM-5V6M75R7` this evening; measured, not assumed — the published container went from 332,030 bytes with no `G-` ID to 378,522 with one, and `last-modified` moved. Policy updated in §§2, 4, 5, 8, 10, 11, 17: `_ga` is a third-party cookie with a per-visitor id and a two-year life, so §10's "apenas cookies necessários" and §5's "com dados agregados" were both false the moment the container was published. §10 now names both cookie categories, states plainly that the metrics **are not anonymous**, and gives two ways to refuse them. **The finding that matters more than the wording: `/radar/edital/…?cnpj=51885242000140&q=papel` is a real address** (pinned in `radar-view.test.tsx:180`) **and GA4 sends `page_location` as the full URL**, so Google is receiving the CNPJ and the search term of every visitor — while §3 of this same policy calls the CNPJ of an MEI personal data "porque costuma conter o nome da pessoa" and §7 promises we never send the CNPJ to the model. Fix is a `page_location` override in GTM stripping `cnpj` and `q`, no deploy, and it must be published **before** #80 merges or the policy trades two false sentences for two others. **Sci's, not mine:** the sentences (brief §5 — he instructed "adequate it to the reality", so I drafted; merging is his authorship); the 14-month retention row, which I wrote **unverified** against GA4 Admin where new properties default to 2 months; whether §15's promised 30 days' notice of a relevant change applies to switching analytics on the night before founders week; and that §8's "não fazemos publicidade com base no seu perfil" holds only while Google Signals stays off. |
| 2026-09-23 | The magic-link e-mail, in Portuguese, with a reply-to | in review | https://github.com/Scintechn/licitaqui/pull/81 | MJ-1a + MJ-2 from `NOTIFICATION_JOURNEY.md` §7, the last open item from that review. Nothing overrode `sendVerificationRequest`, so `@auth/core` sent its own built-in template — **English, "Sign in to www.licitaquiapp.com.br", no reply-to** — on a Brazilian product for MEIs. Invisible until now only because the page *after* the e-mail was broken too: until #77 nobody got far enough to complain about the wording, and fixing the redirect promoted this to the first thing a new founder sees. **A Resend failure raises**, so Auth.js shows its error page rather than the "Link enviado" card — the same rule the rest of today's work turned on, that the app must not claim success for something that happened in another channel and did not. **Nothing remote loads**: no image, no pixel, no external stylesheet, asserted by test — a beacon in a sign-in e-mail is tracking `politica-de-privacidade.md` does not declare, which is a sharper point today than it was yesterday. Both `text` and `html` parts, because text-only lands in spam more often and html-only is unreadable where html is stripped. §12: address and token never logged, never in the thrown message, both tested. 9 new tests including one that goes red if the English default returns and one proving a hostile URL cannot escape the `href`. **63 files / 985 tests**, lint, typecheck, build green. **Not done:** MJ-3 (the card naming the address and the 24 h lifetime — Auth.js does not pass the address to that page, so it is its own change), MJ-4, MJ-5. **Verification gap, stated in the PR:** every check is a test; no e-mail has gone through this code to a real inbox, because `AUTH_MAGIC_LINK`, the Resend key and the verified domain are production configuration. Sci requesting a link on production after deploy is the real check. |
| 2026-09-23 | Honest "abertos", the halted count, and searching again from the Radar | in review | https://github.com/Scintechn/licitaqui/pull/82 | **The landing was overstating.** `stats.ts` counted `proposals_close_at > now()` and nothing else, under a type reading "tenders still receiving proposals": **204 of the 8 028 it called `abertos` had been suspended, revoked or annulled** — one in 39, every one carrying a future deadline, which is precisely why they passed a date test. CDC art. 30 makes an advertised figure binding. Now gated on `Divulgada no PNCP`, the same allow-list `mayShowUrgency` uses, giving 7 824 / 2 627. **The 204 became the third figure** on Sci's approval — at the PNCP you learn a tender was suspended only by opening it; here it is a chip on the card and a number on the front page. It is a **stock, not a rate**: `pncp_updated_at` is the agency's general update timestamp, not a status-change timestamp (all 204 had moved within seven days), so no "N suspensos esta semana" is possible without B10. **Searching again no longer means going home:** the CNPJ was a hidden input in the filter row — carried through every search, editable nowhere — so the one thing the Radar could not change was the company; same `name="cnpj"`, same GET to `/radar`, which already treats `?cnpj=` as a shareable address. The summary now says `changeCompany` ("Trocar empresa ou filtros"), **a string written for exactly this and rendered in zero files**, and `CompanyLine` no longer draws a `chevronRight` inside a `<p>` with no link, button or handler. No new copy: every string already existed and was approved. **989 unit** (986 before), **25 journeys** (23), lint, typecheck, build green; the stats test is mutation-checked (old query → `expected 4 to be 3`), and the journey that clicked `'Filtros'` went red on the rename, which is the suite working. **Carded, not built: B14** (the CNPJ leaves the product in the URL and GA4 records `page_location` — the GTM strip is step one, an opaque search id is step two and must be costed first) and **B15** (a FAQ entry whose honest version is that we *do* keep it, 30 days, for the visitor limit — `windowStartedAt` runs the free window per CNPJ, so the column is the free tier, not analytics). **⚠ #80 merged before the GTM strip**, so `politica-de-privacidade.md` lines 56 and 129 promise the CNPJ is not sent to Google while GA4 receives it on every Radar page view — flagged to Sci the same evening with two remedies. |
| 2026-09-24 | A11y · five serious WCAG 2.2 AA defects from the production audit | in review | https://github.com/Scintechn/licitaqui/pull/85 | **1.4.11** — `--color-line-strong` was the only marker of every form control at **1.55 / 1.45 / 1.34:1** on surface/ivory/fill-muted; all three audit numbers reproduced here exactly. New `--color-field-line: #8a7f6d` (**3.93 / 3.69 / 3.41:1**) on `Field`, `Select`, secondary `Button`, `LockedBlock` and the Radar keyword input. `line-strong` **deliberately unchanged**: it also draws chip, tab and card edges, which are decoration beside an already-legible label and not what 1.4.11 asks 3:1 of. `styles/contrast.test.ts` only ever checked *text* pairs — that gap is how a 1.55:1 border shipped — so it now carries `BORDER_PAIRS` at a **3.0** threshold plus a test recording what the old token measured. **4.1.2** — the filter `<summary>` computed as "Trocar empresa ou filtros **Ordenar: prazo**"; the sort label moves out of the `<details>`. **Deviated from the brief's markup on purpose:** making `<details>` the left half of a flex row also shrinks the full-width form it opens and re-centres the sort label into the middle of the open fields, so the label is instead positioned over the row's right end sharing the summary's box, `pointer-events-none` so the tap target stays full width — screenshotted at 390px closed and open to confirm the row is unchanged. Same control had **no expand affordance** (350×52, marker hidden, one static glyph, and the only way to change the search): `chevronRight` + `group-open:rotate-90`. **3.3.1 / 1.3.1** — `/conta/criar`'s e-mail error was a standalone `StateCard` only, so the field carried no `aria-invalid`/`aria-describedby`; now wired through `Field`'s `error` (the `company-form.tsx:65` pattern), card kept. **Consent tick moved above the Google button**: `required` on it gates both submits, so on a 390px phone the native validation bubble opened below the fold under the button just pressed — LGPD mechanics untouched (one record, not pre-ticked, still `required`, wording not edited — legal brief §5), 16px → 20px. Checks: lint and typecheck clean, **63 files / 1002 tests** (993 + 9), e2e **25 passed / 6 skipped** (the `accounts` project, which skips on absent credentials regardless of this branch); both journeys clicking `'Trocar empresa ou filtros'` pass. **Follow-up for whoever picks it up:** `Button` variant `locked` still draws `border-dashed border-line-strong` on `fill-muted` — the same **1.34:1**, on a control that is focusable by design — left alone because it was outside the four files the card named |
